## 1.0.1 — 2026-09-24（OTA 修复版本）

- 更新下载弹窗显示实时进度、速度和已下载大小。
- OTA 更新源切换到 `zmjza/SmartAnswerPod`。
- Windows NSIS 安装包创建桌面快捷方式和开始菜单快捷方式。
- macOS 继续使用 Squirrel.Mac 在已安装应用位置替换旧版本并重启。

## 1.0.0 — 2026-09-24（正式版本）

- 正式发布开大智达舱桌面安装包与 GitHub OTA 更新清单。
- 保留 macOS Squirrel.Mac 下载完成后自动安装并重启；Windows x64 提供 NSIS 安装包。
- 发布验证：macOS ARM64、Windows x64 均通过 GitHub Actions 构建、资产摘要与回下载校验。

## 0.9.0 — 2026-09-24（OTA 验证版本）

- 用于验证 0.8.0 正式客户端从 GitHub 更新源发现新版本。
- 保留 macOS Squirrel.Mac 下载完成后自动安装并重启，以及 Windows x64 NSIS 更新清单。
- 验证范围：GitHub Actions macOS ARM64 / Windows x64 构建与资产校验；0.8.0 客户端在线检查、下载、安装和重启将在本版本发布后记录。

## 0.8.0 — 2026-09-23（已发布）

- 发布产品名称“开大智达舱”；macOS 与 Windows 打包图标均由仓库现有 logo 生成。
- 顶栏展示当前版本徽记；点击更新检查时显示旋转状态，最新版显示通知，新版以品牌弹窗提供版本对比、下载和安装重启操作。
- 将 OTA 更新源改为 `zmjza/SmartAnswerPod`；修复更新服务明确返回无新版时仍显示可下载的问题。
- macOS 明确启用 Squirrel 安装后的应用自动重启；自动安装与用户数据保留仍待发布版真机验证。
- 验证：OTA 流程测试与构建通过；Squirrel 自动安装、重启、用户数据保留及本次 ad-hoc 签名安装包仍待真实发布客户端验收。
- 限制：macOS 使用 ad-hoc 签名，不具备 Apple Developer ID 信任或公证；首次打开可能出现 Gatekeeper 提示。GitHub Release 和在线 OTA 尚未验证。

## 0.1.7 — 2026-09-23（开发中记录）

- 实施 CHG-026 四页 Indigo 壳 UI 增量：学生批量粘贴导入、课程分页、学生切换、窗口控制、OTA 检查入口和品牌更新。
- 按用户截图将设置页“运行环境与调试”移至左列“云端与接口连接”紧下方，保留四个主页面顶部间距基线；用户 Logo 改为项目内构建资源。
- 学生账号卡片桌面端调整为一行三列（移动端单列）；批量粘贴导入与课程分页使用隔离模拟数据 E2E 验证，用户已确认本轮 UI 无问题。
- 工作台溢出学生“…”菜单移入最后一行学生项并靠右显示；账号 E2E 验证末行垂直对齐和右侧位置。
- OTA 接入 GitHub Release 检查、用户确认下载、下载完成安装重启、运行中安装保护和错误重试；macOS 配置 DMG/ZIP，Windows 保留 NSIS。
- 修复单个/全部停止完成后未主动广播最终快照的问题；真实 3 账号并发 1 复测确认排队补位、全部停止、运行态释放、按钮禁用和窗口保留。
- 验证：构建通过、P0 75/75、CHG-026 隔离 UI E2E、账号页面 E2E 和 OTA 流程测试 4/4 通过；题库导入清理请求 HTTP 401 按用户指示忽略。
- 保持现有答题、提取题库、浏览器池、课程队列、并发、状态机、扫码、提交和题库回写逻辑不变；本版本不提交、不推送、不发布。

# 更新记录

## 0.1.6 — 2026-09-20（开发中）

- 完成工作台状态、全部/可选课程范围、历史成绩、课程日志、运行锁、真实无头/有头浏览器模式、AI 全模型连通性、题库分页查询/删除/导入和本机 metrics 接线。
- 修复题库查看解锁后的课程下拉加载竞态：课程分类先完成读取，再加载题目列表，避免总数先出现但课程选项为空。
- 修复题库去重读取的瞬时网络失败：云端题目读取增加一次有界重试；持续失败仍进入待回写并显示失败统计。
- 真实无头和有头单账号提取均已跑通；无头 49 次显隐采样均不可见，有头复测 50 次均可见，结束后浏览器数均回到 0。
- 最新单账号复测：无头完成 4 门课程、7 份作业、20 页历史，44 次显隐采样均不可见；有头同样完成，50 次显隐采样均可见；两次浏览器数均回到 0，提取统计逐项求和一致。
- 修复真实浏览器上下文异常关闭后的名额泄漏：上下文关闭事件会清理持有表、浏览器样本和槽位，并补充杀掉真实浏览器后的释放回归。
- 修复学生状态展示重复拼接当前动作：主状态只显示账号状态，当前动作和进度分别保留在对应区域。
- 补充设置保存失败回归：非法配置保留旧值且不广播新快照；连通性弹窗回归检测时间显示。
- 修复浏览器池推荐并发只读取页面 JS 堆、在 Chromium 中长期回退 512MB 的问题；现在按每个持久化 profile 定位真实 Chromium 进程树并采集 RSS，推荐值明确区分真实样本与保守估算。
- 新增真实浏览器池 E2E：账号并发 2 时两个浏览器占位、其余账号排队且不计入浏览器数，压力和浏览器平均内存来自运行中真实快照。
- 区分无浏览器样本与运行中采样失败：前者显示保守估算，后者显示 unavailable，避免把回退值误报成过期浏览器均值。
- P0 70/70、Vue/Electron 类型检查、Vite/Electron 构建通过；真实手机扫码及扫码后答题链路保持待测。
- 真实答题模式由站点“做作业”入口触发二维码：无头和有头均进入 needs_verify；程序内查看、脱敏字段、PNG 复制、刷新旧码失效、关闭弹窗、未确认阻塞和隐私检查通过，手机扫码后的恢复与提交继续待用户协助。
- 修复账号 E2E 对共享持久化浏览器默认设置的污染：测试显式准备并恢复 browser_visible_default，不改变新增账号继承设置默认模式的产品规则。
- 修复真实答题二维码竞态：主进程在二维码内存快照准备完成后才发布 needs_verify，避免界面已显示验证态但程序内二维码仍暂不可读；无头/有头答题+全部课程二维码前探针均通过。

## 0.1.5 — 2026-09-20（开发中，未发布）

- 批阅历史列表未及时给出总分时，改为从批阅卷每题 `GiveScore` 实得分求和，补齐工作台真实本次得分。
- 新一轮开始后保留上一轮已批阅分数并明确标为“上次”，避免正在连刷时分数瞬间消失。
- 新增实得分求和和跨轮显示回归；产品 E2E 仍继续运行验证。

## 0.1.4 — 2026-09-20（开发中，未发布）

- AI 全局请求启动间隔从 1.5 秒调整为 0.75 秒，降低正常多模型降级的等待；HTTP 429 仍按 `Retry-After` 或至少 5 秒冷却。
- 题库答案成功回填并确认页面已选后，随机等待 1–3 秒再进入下一题；补答命中题库时执行相同节奏，AI 回填不重复叠加等待。
- 新增 AI 节流与题库随机节奏离线回归；真实站点节奏、429 恢复和完整提交链路仍待新版产品 E2E。

## 0.1.3 — 2026-09-20（开发中，未发布）

- 硅基流动降级模型合并去重为 14 个固定顺序，所有模型共用统一高校课程客观题提示词并只接受 `option_texts`。
- 全部模型失败时，工作台显示具体题号、已尝试模型数和最终失败原因，不再无说明结束。
- 漏题补答上限从两轮提高到 30 轮；每轮重建整卷复核计划并显示进度，第 30 轮仍失败时列出剩余题号，安全停止且不提交。
- 修正状态汇总重复追加题号的问题；新增对应离线回归。
- 所有学生共享 1.5 秒 AI 请求启动间隔；HTTP 429 按 `Retry-After` 或至少 5 秒冷却，避免多账号突发限流。
- 验证：P0 48/48、主进程与渲染进程类型检查、Vite/Electron 构建通过；真实站点 14 模型、限流恢复与 30 轮故障路径仍待产品 E2E。

## 0.1.2 — 2026-09-19（开发中，未发布）

- 作答开始前验证整卷题干与选项可读；提交前复核题量、唯一题号、隐藏答案完整集合和右侧题号已做状态。
- 提交前发现漏题时最多补答两轮并按补答后的最新选择重建复核计划；仍未补齐则停止该份作业。
- 提交弹窗出现“部分题目没有作答”时自动点击绿色取消，禁止提交漏题卷；正常完整卷只确认“作业提交后将不可修改”。
- 增强产品 E2E 状态摘要，记录当前动作、轮次和本次得分，便于真机定位失败阶段。

## 0.1.1 — 2026-09-19（开发中，未发布）

- 作答及题库写入口拦截缺失或含 Unicode 替换字符的题干、选项及入库答案，避免损坏文本用于新一轮作答或污染题库；已有异常待回写仍保留待处理。

- 工作台每份作业新增本轮作答次数及本次真实得分；保留零分、小数和待批阅状态，新一轮清除旧分数。

- 批阅页读取同时支持单多选 e-choice-a 与判断 e-checking-a，避免漏读选项导致无法回写。
- 提交前整卷复核数量、唯一题号及每道题实际答案集合，回填丢失或多选不完整阻止提交；构建加入 Electron 主进程类型检查。

- 每个学生按门户课程顺序处理全部可做作业；当前作业完成提交、校对和回写后再处理后续作业。待回写阻止同学生继续新开，保留满分、时间窗、次数限制与非客观题跳过规则。
- 题库错误答案删除后重新读取验证，保护其他学生已纠正的答案；正确题去重入库，冲突与失败保留待处理。
- 移除工作台全选执行、全选未完成，点击课程只切换查看对象，不限制执行范围。
- 逐题回填更新主进程课程/作业快照和来源计数；移除渲染层旧计数回填及临时调试上报。
- 收尾汇总失败、未满分、待回写，不再一律显示本轮结束而隐藏未完成项。
- 修正待回写阻塞键分隔符，避免未补偿完毕又开新卷；作答页查找限定当前作业。
- 修正答案读取 CDP 兜底表达式转义并释放会话；时间窗或次数耗尽的课程标记暂不可完成。
- 提交成功证据排除草稿与未提交记录，统一时间格式；校对沿用实际找到新提交记录的页面。
- 单门课程检测失败保留失败状态并继续其它课程；新增脱敏校对计数帮助排查待回写。
- 验证：离线回归和构建通过；全课程真实站点链路仍在复测，未宣称 E2E 全通过。
