# 开发追踪

- 所有学生共享 AI 请求启动节流，最小间隔 1.5 秒；不新增依赖。
- HTTP 429 读取 `Retry-After`，缺失或过短时至少冷却 5 秒；后续排队请求在实际执行前重新计算等待时间。
- P0 已覆盖共享间隔、429 冷却和无效 `Retry-After`；真实站点限流恢复仍待产品 E2E。

## 2026-09-20 CHG-013 作答节奏优化

- 普通 AI 请求启动间隔从 1.5 秒调整为 0.75 秒；所有学生仍共享节流器，HTTP 429 仍按 `Retry-After` 或至少 5 秒冷却。
- 题库成功回填并确认页面已选后随机等待 1–3 秒；补答命中题库时相同，AI 回填不额外叠加该等待。
- 离线 P0 49/49 已覆盖 0.75 秒、429 冷却及 1–3 秒边界。旧产品会话已停止，真实站点节奏与待回写匹配 0 问题仍需新版产品 E2E。
- 待回写匹配 0 根因已由真实只读探针定位：history.aspx 富文本块用 `textContent` 会丢失可见段落间空格。校对读取改为优先 `innerText`；新版产品实测由“45 题匹配 0”恢复为“匹配 1、可判定 1”，保留队列完成补偿后继续扫课和答题。
- 新版双账号产品会话继续运行：AI 多模型降级能够继续下一题，题库命中按 1–3 秒节奏持续推进；第二学生连续两轮完成 45/45 整卷、第一学生完成 50/50 整卷，均正常提交并在 history.aspx 全量校对匹配后自动再开。本轮 P0 49/49、双类型检查、构建和 diff-check 通过。真实 429 冷却和漏题补答节奏仍待可控故障复测，development 3/4 不提前收口。
- CHG-014：批阅历史总分为空时，从 history.aspx 每题 `GiveScore` 实得分求和；进入下一轮后保留上一轮分数并明确显示为“上次”。离线 P0 50/50、双类型检查、Vite/Electron 构建和 diff-check 通过。旧运行会话已真实读到第 9 次 93 分；新版跨轮保留显示待下一次产品会话复测。

## 2026-09-20 CHG-018～CHG-025 无扫码真机增量

- macOS 隐藏式无头已用原生进程可见性闭环修复：一个账号无头全量提取 44 次采样均隐藏，可视化全量提取 42 次采样均可见；同一根 PID 完成 `headless → visual → headless`，未重建浏览器上下文。
- 全部提取已用 7 个唯一账号完成 104 份作业；可选课程提取真实扫描 4 门课程后停在等待选择，只处理选中的 1 门课程 2 份作业和 20 条历史，另外 3 门课程未进入队列。
- 2026-09-21 最新 7 个唯一真机账号复测：全部提取 104 份作业，新增 0、去重 4366、跳过 861、冲突 0、写入失败 0；7 个学生均 round_ended，320 次无头显隐采样均为隐藏。产品 E2E 必须显式设置 KAIDA_PRODUCT_RUN=1 才进入真实调度，避免把“只启动应用、未启动任务”误判为产品失败。
- 可选课程五项总计为新增 0、去重 1010、跳过 211、冲突 0、写入失败 0，与两份作业明细逐项相加一致。
- 查看题库 E2E 已验证三组固定密码、五次错误冻结、跨重启保持、到期恢复、课程筛选、模糊和精确搜索、单删与批删回读。批删部分失败以本批测试题受控 HTTP 503 验证：成功项删除、失败项保留并显示原因，恢复后本批数据回读为 0。
- 2026-09-21 课程选择修复后再次运行 7 个唯一真机账号的无头提取：104 份作业全部进入明确终态，新增 0、去重 4356、跳过 862、冲突 0、写入失败 9；7 个学生均 round_ended，377 次窗口显隐采样均为隐藏。该轮证明失败统计和“有待处理项”收口真实生效，但写入失败项仍需后续补偿复测，不能记为全成功。
- 本阶段仍为 development 3/4；A～I 其余无扫码用例继续逐项实测，J01～J18 保持待条件。
2026-09-21 追加实证：学生状态主字段改为只显示 ACCOUNT_ZH 主状态，当前动作继续单独显示；`node tests/e2e-workbench.mjs` 增加真实快照断言，确认“等待选择课程”不再重复拼接“课程检测完成，请选择本轮课程”。

## 2026-09-22 CHG-018～CHG-025 无头/有头与工作台增量复测

- 产品构建后串行完成无头和有头一账号真实提取：两轮均完成 4 门课程、7 份作业、20 页历史，作业明细、学生统计和运行总计一致；无头窗口采样保持隐藏，有头窗口采样保持可见，结束后浏览器数均归零。
- `tests/e2e-workbench.mjs` 先红后绿补充真实工作台证据：两个学生切换不串号、两门课程切换后标题和课程日志同步、主状态与当前动作分离、题库/AI 计数按快照刷新、提取课程/作业进度与五项统计区域真实显示。
- 串行回归通过：`npm run test:e2e:run-lock`、`node tests/e2e-course-selection.mjs`、`node tests/e2e-browser-pool.mjs`；浏览器池结果为账号并发 2、占用 2、排队 1、浏览器 2，使用真实浏览器 RSS 样本约 1072MB。
- 课程选择 E2E 输出 `multiSelect=true`、`notification=true`：首个真实学生选择 2 门课程、第二个选择 1 门，提取总课程数与快照一致；通知探针捕获“课程检测完成”，停止后可编辑且第二轮会清空旧选择。
- 题库页面新增的首页、七列表格、时间格式、长题干展开和分页断言尚未用正确的三个门禁密码完成复测；当前只保留原有题库 E2E 历史证据，不把本次未完成的新增断言记为通过。
- 工作台 E2E 先红后绿修复未选课程日志门禁：检测到课程但尚未选课时，日志按钮不再因回退到第一门课程而误启用；复测同时通过课程日志切换、颜色级别、敏感字段过滤、可见按钮动作和提取中快照对齐。
- 2026-09-22 设置页真实复测：14 个模型、峰值并发 3、10 成功/4 失败（超时或空响应），单模型重测、保存失败回滚、设置广播、机器采样和弹窗关闭通过；不改变正式模型降级顺序。
  - 2026-09-22 继续复测：工作台课程选择先红后绿修复渲染层课程选择值被异步活动课程覆盖的问题；主进程返回最终 selectedNames，渲染层按主进程结果同步当前课程。工作台最终通过学生/课程日志切换、取消当前课程后回退、历史成绩和提取快照；设置页新增“当前配置高于建议”真实警告断言并通过；学生账号页新增 E2E 通过新增、编辑取消/保存、工作台切换、明文显示和删除回收。
- 历史记录：当时为 development 3/4；扫码后 J01～J18 后续已按最终验收口径收口，T001 已于 2026-09-24 完成用户验收并归档。
- 2026-09-22 阶段性测验历史成绩补测：真实工作台入口可点击并完成历史读取，返回 1 条 `no_view` 记录；仅回写 T15-C02 及无查看链接的自然证据，暂无记录、未批阅、未完成、续做、读取失败和只读答卷仍未标绿。
- 2026-09-22 工作台长状态修复：主状态和当前动作增加两行截断、完整文本 title 和 min-w-0 布局约束；E2E 先红后绿，重建后工作台真实复测通过。
- 2026-09-22 课程选择 UI 真机补强：修正测试原先直接调用 setSelectedCourses 和 startSelectedCourses 的覆盖缺口，改为 Patchright 点击学生切换菜单、课程卡片和“开始执行”；真实通过双学生独立选课、多选、未选排除、队列范围、停止后编辑和重新扫描清空旧选择。
- 2026-09-22 当前扫码前进度：T15-A～I 110/132（83.3%）；未触发的网络、限流、服务端故障项不虚标通过，题库合法门禁后的分页、七列表格和长字段断言仍待复测。
- 2026-09-22 题库门禁错误次数补测：独立 Patchright 真机连续输入四个错误值，剩余次数 4、3、2、1 均按界面真实显示；未使用或泄露合法门禁密码。
- 2026-09-22 历史成绩 no_view 补强：工作台真机 E2E 对阶段性测验无可靠查看入口增加界面断言，确认按钮显示明确原因且 disabled；T15-C07 通过。
- 2026-09-22 题库导入真机补测：导入脚本启动产品后补接真实 saveSettings；使用独立批次通过新增、重复、跨课程合并、选项乱序、冲突、缺字段、空数组、非法 JSON、写入失败、回读失败和非本批保护，最终批次回读为 0。
- 2026-09-22 继续串行复测：P0 70/70、构建、工作台、运行锁、真实课程选择、无头/有头切换、浏览器池、设置页和学生账号页均退出码 0；无头/有头启动与真实 RSS 采样证据保持有效。扫码前进度仍为 T15-A～I 110/132（83.3%），不因未完成题库门禁后的 UI 断言和历史异常分支而虚增。
- 2026-09-22 历史状态探针先因固定作业名定位失败，随后改为遍历真实快照课程/作业并完成 UI 复测；viewable 真实打开只读历史答卷并加载截图，unfinished 显示未完成且按钮 disabled，no_view 显示无可靠查看入口且 disabled。T15-C06、T15-C09 现有真实证据，暂无记录、未批阅、仅续做和读取失败仍待自然出现。
- 2026-09-22 本轮动态历史复测后，扫码前 T15-A～I 进度更新为 112/132（84.8%）；仍保持 development 3/4，不进入 acceptance。
- 2026-09-22 继续串行复测：`npm run build`、`npm run test:p0`（70/70）、`node tests/e2e-workbench.mjs`、`node tests/e2e-course-selection.mjs`、`npm run test:e2e:run-lock`、`node tests/e2e-display-toggle.mjs` 和 `npm run test:e2e:settings` 均退出码 0。课程选择仍由真实页面点击课程卡片和“开始执行”进入队列；无头/有头模式、崩溃释放、运行锁和设置失败回滚没有回归。设置页本轮 14 个模型为 12 成功/2 失败，峰值并发 3。
- 2026-09-22 真实二维码链路补测：使用新版 Electron 答题模式，真实登录后进入 4 门课程并点击真实“做作业”，无头与有头均进入 `occupying_verify / needs_verify`。程序内二维码快照、脱敏课程/作业字段、PNG 复制、刷新版本递增、旧版本失效、未扫码验证拦截、关闭弹窗后保持验证占位和停止释放均通过；扫码前进度更新为 T15-A～I 119/132（90.2%）。扫码后的手机确认和作答提交仍留待用户协助复核。
- 2026-09-22 稀有故障项按用户口径收口：T15-D04、D05、D13、G21 的实现、P0 或受控失败语义已验证；未自然出现的鉴权、网络中断、服务端、限流和 metrics 采样故障不伪造为现场证据，记录为后续用户现场复核项。扫码前统计按实现/受控口径更新为 123/132（93.2%）。
- 2026-09-22 当前工作区回归：npm run build、npm run test:p0（70/70）、npm run test:e2e:workbench、node tests/e2e-course-selection.mjs、npm run test:e2e:run-lock、node tests/e2e-display-toggle.mjs、npm run test:e2e:settings、npm run test:e2e:bank-import 和修复后的 node tests/e2e-accounts.mjs 均通过。账号 E2E 显式准备并恢复持久化浏览器默认设置，避免显示模式测试污染新增账号默认值；不改变产品“新增账号继承当前设置”的规则。
- 2026-09-22 工作台历史异常遍历复测：真实扫描两个学生的所有可读取历史作业，viewable、unfinished、no_view 通过 UI；本轮输出 emptyHistoryChecked=false、failedHistoryChecked=false，当前账号未自然出现暂无记录或无可靠历史链接作业，保留产品错误/空态分支供用户现场复核。
- 2026-09-22 B02 收口：使用一个真实账号分别以 headless:true 和 headless:false 运行答题+全部课程，真实进入 4 门课程并点击真实“做作业”到 occupying_verify / needs_verify；程序内二维码截图、课程/作业字段、PNG 复制、刷新版本递增、旧码失效、未扫码阻塞、关闭弹窗和停止释放均通过。修复主进程先发布 needs_verify、后异步写入二维码快照的竞态，现改为二维码快照准备完成后才发布验证态；扫码前 T15-A～I 按实现/受控口径更新为 132/132（100%）。
- 2026-09-22 回归收口：构建、P0 70/70、工作台、运行锁、课程选择、设置、无头/有头答题二维码探针均通过；工作台和运行锁并行执行曾因共享 Electron userData 互相抢运行态而超时，按既有避坑规则串行复测通过。题库合法门禁后的 E07/E12/E13/I02 仍保留用户现场复核备注。
- 2026-09-22 用户确认扫码前范围按当前口径收口，v1.16 续作提示词转入 T15-J01～J18 真实扫码后验收；J 全部通过后必须重新串行回归 T15-A～J，再按 1.0.0 发布门禁执行，不新建 Goal、不修改 T001 的 3/4。
- 2026-09-22 可选课程+无头+并发1顺序复测：第一账号真实通过课程检测、页面选择1门课程、无头答题、两轮提交/校对并进入 round_ended，未选课程均保持“本轮未选择”，浏览器释放为0；第二账号接续检测时因真实站点/网络导致部分课程检测失败，没有可选客观题课程，按门禁停止，未伪造选课通过。
- 同轮修正 tests/e2e-product.mjs：顺序切换调用 loginRefresh 改为非阻塞触发，选课汇总改为等待真实 DOM 文本，避免测试驱动卡住或抢在 Vue 广播前断言；KAIDA_KEEP_OPEN=1 下新增异常保持 Electron 窗口的处理，待下一次失败场景再验证。
- 本轮仍为 development 3/4；未提交、未推送、未发布，扫码后整体回归继续待真实网络稳定后执行。

## 2026-09-22 CHG-026 UI 增量目标

顶部布局基线（2026-09-23 用户确认）：四个主菜单页面的主内容顶部间距按当前 Electron 界面保留，不再自行调整；后续 CHG-026 接线只允许修改功能、显示内容和交互，不改变这条间距基线。

### TASK-U01 四页壳 UI 增量与轻量接线 — REQ-032 至 REQ-038

- [x] U01-01 空状态靠左、桌面端三列学生卡及粘贴解析/预览/二级确认已接线；隔离模拟 E2E 验证取消不写入、确认后安全存储回读 2 条、清理后 0 条。
- [x] U01-02 公共 Supabase 配置沿用本机常量，页面隐藏两项输入；左列云端卡下紧接运行环境卡，右列保留并发卡；用户已确认 UI 无问题。
- [x] U01-03 工作台按账号并行逐行显示学生、超出项“…”菜单固定在最后一行学生右侧；真实账号 E2E 验证同一行对齐及菜单交互，用户已确认 UI 无问题。
- [x] U01-04 底部固定栏、运行中登录刷新禁用、单个/全部停止入口已接线；真实 3 账号、账号并发 1 复测确认 1 个占用、2 个排队，停止占用账号后排队账号接续；全部停止后运行态释放、按钮禁用且窗口保留。
- [x] U01-05 课程固定 360px、图标分页、手动往返及当前课程第 3 页跳至第 1 页已由隔离模拟 E2E 验证。
- [x] U01-06 提取题库模式隐藏作业重新答题次数已接线，用户已确认 UI 无问题。
- [x] U01-07 窗口按钮、启动/手动 OTA 入口和品牌标题已接线，Logo 已收为项目内构建资源；用户已确认 UI 无问题，OTA 状态机内部测试通过；真实 Release 安装态待发布后验证。
- [x] U01-08 P0 75/75、构建、隔离 CHG-026 E2E、账号 E2E 和 OTA 流程测试通过；题库导入 E2E 的 HTTP 401 按用户指示忽略。

验证责任：U01-01 和 U01-05 由 Codex 用模拟数据 E2E；其余 UI 已由用户确认。实现开始前将版本从 0.1.6 升至 0.1.7 并更新 CHANGELOG。完成后停止任务并保留 Electron 界面，等待用户确认，禁止自动提交、推送或发布。
2026-09-23 工作台学生“…”布局调整：旧实现将菜单放在所有学生行之后；现移入可见学生的最后一行并靠右。`node --experimental-strip-types tests/e2e-accounts.mjs` 通过对齐、右侧位置和账号增删回读；`npm run build`、`npm run test:p0`（75/75）、`npm run test:e2e:chg026-ui` 通过，用户确认 UI 无问题。题库导入 E2E 的 HTTP 401 按用户指示忽略。
2026-09-23 验证：`npm run build`、`npm run test:p0`（75/75）、`npm run test:e2e:chg026-ui`、`node --experimental-strip-types tests/e2e-accounts.mjs`、`node --experimental-strip-types tests/ota-flow.test.mts`、`KAIDA_E2E_USERDATA=/tmp/kaida-u0104-20260923 npm run test:e2e:run-lock` 和 `git diff --check -- .` 通过。OTA 已覆盖开发版跳过、检查、下载失败重试、运行中禁止安装和安装重启调用；macOS DMG/ZIP 与 Windows NSIS 配置校验通过。题库导入 HTTP 401 按用户指示忽略。development 3/4 继续进行，未进入 acceptance 4/4、提交、推送、发布或归档。
2026-09-23 U01-04 真机复测：使用 3 个用户提供的测试账号和隔离加密目录，账号并发 1、课程并发 1。Patchright 真实启动后输出 `queuedBefore=2`、`replacementStarted=true`；点击全部停止后输出 `runningReleased=true`、`buttonDisabled=true`、`windowRetained=true`。复测发现停止 IPC 完成后缺少最终快照广播，已在 `electron/main.ts` 补充 `pushSnap()`，重新构建并通过 `npm run test:e2e:run-lock`。
无。UI 2/4 Stitch 已回传并复修。

## 文档目标审计

- 产物：00–10、modules A–M 叶子、ui-shells 三份骨架、archive 空索引。
- REQ-001 至 REQ-023、AC-001 至 AC-024 均出现在 modules 或本文件微观任务。TASK-T01 为本轮新增。
- 文档 1/4 当时未改业务源码。development 已改 electron/src/tests；未覆盖 AGENTS.md 旧正文；未写入密钥。
- 下一阶段解锁条件：development 离线 P0 与 Playwright E2E 全绿（或缺 DOM 受阻已写明）后，才生成 acceptance 4/4。4/4 不再用 @电脑。本阶段不得宣布整体完成，不得归档。

## 2026-09-18 实时反馈增量

- 主进程 StudentView 已维护学生课程、课程作业分组、作业状态、题目来源统计和当前活跃课程，并在状态变化时立即推送完整快照。
- 主界面左栏“我的课程”改为读取实时课程快照；课程检测经历“待检测 → 检测中 → 已检测/作答中”等状态时，课程行即时更新。
- 中栏按当前选中课程读取实时的网上记分作业与阶段性测验，不再使用固定演示作业；每份作业显示状态、进度和题库/AI/空过来源统计。
- 已通过 npm run test:p0（20/20）、npx vue-tsc --noEmit、npx vite build、git diff --check -- .。独立 Electron 冒烟未形成有效运行态证据，不能替代产品 Playwright E2E。

## 2026-09-18 题库与设置 UI 样式恢复

- 发现题库导入页和设置页曾显示为无样式简化内容：真实 Vue 模板仍在，但 Tailwind 工具类只由远程 CDN 提供，Electron 在网络不可用或 CDN 未加载时只剩原始 HTML 排版。
- 已改为本地 Tailwind + PostCSS 构建，保留 Stitch 主题色、字体、圆角、阴影和间距配置；src/views/BankImportView.vue、src/views/SettingsView.vue 的真实接线和页面结构未删除。
- 验证：npm run build 通过，产物包含本地 Tailwind 工具类；未接入新的业务接口，未改答题流程。

## 2026-09-19 全课程与实时反馈修复

后续修复：校对先等待题目及完整答案匹配（最多8秒，仍不匹配保留待回写），云端修改前安全保存候选；本地待回写保存失败抛错阻止继续。作答／提交未确认时停止该学生后续作业。新版41955真实补偿仍有3条题干身份不匹配，未写入或删除这些不确定题目，仍需定位；不得把有限等待标为已经修复此问题。

最新真实证据：77055 中第二账号历史候选 50/50 匹配并解除补偿，进入全部6门检测及第一份40题作答提交校对；第一账号残留2条候选不匹配被门禁拦截。仍未完成全部课程及全矩阵，不可记完成。新增整卷提交前复核和 G-ALL-17/18；当前运行进程尚未加载整卷复核增量。

用户确认 CHG-009 后恢复 development 3/4。新增删除后读回验证、保护并发纠正答案、待删 hash 禁用、正确题去重回写；启动补偿未结束时立即阻止该学生进入新作业，运行中待回写也阻止下一课程。09 新增 G-ALL-11 至 G-ALL-16，未预填真实通过。此前 32762 继续其它课程的行为不满足新门禁，不再作为门禁通过证据。

后续增量：提交证据现在排除未交卷/续做记录并统一日期比较，校对沿用实际命中新记录的页面；新增脱敏校对匹配计数。单门课程检测失败不会中断其余课程，也不会归为无作业。P0 24/24、构建和 diff-check 通过。运行中的 32762 尚未加载本增量，不能把旧进程证据归于新实现；仍需完整真实复测。

当前目标仍为 development 3/4。修正原因：同号并发点击作业触发未验证闸门，兄弟任务直接返回失败；逐题事件未更新课程快照。改为按课程和作业顺序执行，逐题确认后更新快照，最后聚合未完成和待回写。全部课程按钮改为说明文本，保留 Stitch 样式。P0 23/23 与构建通过，产品全课程 E2E 尚待新版复测。已保留两个账号 local_id 与 profile。
# 2026-09-19 当前增量：得分与文本完整性

development 3/4 继续，未收口。每份作业中栏已接本次轮次及已批阅分数，未出分不伪造0分，新轮清除旧分。G-ALL-19真实界面待核验。当前账号3条残留候选中2条题干含替换字符，1条身份可匹配；新增文本完整性拦截，旧记录未猜测修复或清空。离线27/27通过；全课程真实链路仍未通过，不解锁4/4。

## 2026-09-19 CHG-010 防漏提交增量

- 用户现场确认旧运行存在右侧题号未提亮仍进入提交。最新规则覆盖早期“空题仍提交”：自动答题不可提交空题。
- 已实现作答前整卷文本预检、提交前隐藏答案与右侧 active 双检、多选完整集合校验、最多 30 轮补答、补答后重建最新复核计划。
- 提交弹窗出现“部分题目没有作答”时自动点绿色取消，禁止继续确认；正常完整卷仍点深蓝确定，最终成功只认历史有效新记录。
- 本地 Chromium 回归已覆盖损坏文本有限重载、隐藏答案、右侧 active、多选少选/多选、判断值 0 和补答计划刷新；最新版真实站点 G-ALL-22 至 G-ALL-26 尚待复测。本阶段未完成。

## 2026-09-19 独立提取模式复跑验证

- 独立提取模式第二轮真实运行已完成：两个学生共扫描 5 门课程、处理 5 份符合范围作业；全部作业状态为 extracting_done，两个学生均以 round_ended 收口，浏览器占用从 2 释放到 0。
- 第二轮结果为学生 1：新增 0、去重 185、跳过 59、冲突 0、写入失败 0；学生 2：新增 0、去重 120、跳过 3、冲突 0、写入失败 0；总计：新增 0、去重 305、跳过 62、冲突 0、写入失败 0。
- 已将提取 E2E 收口断言加入 tests/e2e-product.mjs：学生五项统计必须等于其作业明细之和，所有符合范围作业必须结束后才允许 round_ended；本轮 extract-summary-verified 通过。
- 本次只证明独立提取模式的真实扫描、历史读取、正确题去重写入、并发占位和统计收口；自动答题提交、AI 正确题入库、题库错误答案删除、提交后校对等矩阵项目继续保持待实测。

## 2026-09-20 CHG-011 多模型与补答上限增量

- AI 候选已按用户清单与原候选合并去重为 14 个固定顺序，全部复用统一高校课程客观题提示词，只接受 `option_texts`。
- 单轮所有模型失败时，主界面状态包含题号、已尝试模型数和最终失败原因；共享状态格式化会避免重复追加同一题号。
- 漏题补答上限改为 30 轮。每轮显示当前轮次，补答后重建整卷计划；第 30 轮后仍有漏题时列出剩余题号和原因，安全停止并明确显示未提交。
- 离线 P0、类型检查与构建结果以本轮最新命令为准；真实产品 E2E 尚未证明 14 模型降级和 30 轮耗尽路径，本阶段未完成。

## 2026-09-20 CHG-012 AI 请求节流增量

## 2026-09-24 T001 正式归档

用户确认“都没问题。验收完毕”，T001 用户验收通过并完成正式归档。归档包：`liran_docs/archive/T001-开大自动答题桌面端/归档.md`。版本 0.1.7 的提交、双远端推送和 GitHub Release 仍属于独立发布操作。

## 2026-09-23 OTA 收口与 UI 验收

历史记录：用户已确认 UI 阶段无问题，顶部间距继续冻结。CHG-026 UI 与 OTA 接线完成；OTA 离线流程测试 4/4、P0 75/75、构建、CHG-026 UI E2E 和账号 E2E 通过。题库导入 HTTP 401 按用户指示忽略；真实 GitHub Release 安装仍待发布后验证。随后 T001 完成用户验收并归档。

## 2026-09-22 用户最终真机确认

- 用户确认 T15-J10“全部课程答题”和 T15-J18“未满分进入下一轮并逐轮即时回写”已完成真机实测，前述旧批次课程未完成记录不再作为当前阻塞。
- 用户同时确认扫码后范围及整条业务链路均无问题；按用户要求不重复执行整套 T15-A～J 回归。
- J01～J18 现均按当前验收口径具备通过依据；当前目标为 development 3/4，已按用户要求暂停并回收，等待下一步 UI 美化指令；不进入 acceptance、归档或修改目标次数。

## 2026-09-22 并发 1 顺序复测

- 产品设置真实加载 `account_parallel=1`；学生 1 占用唯一浏览器时，学生 2 保持 `queued / 等待浏览器名额`，学生 1 释放后学生 2 自动接续，证明顺序调度和浏览器名额释放成立。
- 学生 1 在 EBSCOhost 作业提交阶段出现 `submit_failed`；用户确认当时家庭网络中断，按外部网络故障记录，不归因于并发逻辑。学生 2 随后出现多门课程检测失败，暂按同一网络条件保留，网络恢复后复测。
- 本轮未满足完整顺序答题通过条件；程序窗口保持逻辑仍需单独验证，当前目标继续为 development 3/4。

## 2026-09-22 扫码后双并发收口

- 用户完成两个学生的真实手机扫码，并在产品页面分别点击“验证完毕”。可视化运行完成真实题目读取、题库/AI 回填、提交、分数与历史校对；随后无头双并发复用两个持久化 profile，两个学生均进入 `round_ended`，最终 `running=false`、`browsers=0`、`needsVerify=false`。学生 2 的 2 门未完成课程按真实站点状态保留，未伪造成功。
- 用户补充既有真机证据：J02、J03、J04、J06、J07、J17 已通过；J12、J13、J16 按用户确认的口径，若本轮真实运行至结束仍未自然出现，则默认通过并在报告中保留判定备注。
- 当前 J 状态：J01～J18 均有通过依据；J11 已补充严格页面选课后的扫码后双并发证据。
- 本轮无头双并发输出 `answer-summary-verified`，学生数 2，两个终态均为 `round_ended`；可选课程有头双并发复测也输出 `answer-summary-verified`，两个学生均只遍历 1 门所选课程，浏览器数回到 0。当前仍为 development 3/4，不进入 acceptance、归档、commit、push 或 release。

## 2026-09-21 PRD 1.15 增量任务（当前目标执行中）

状态口径：以下新增任务均为已接线、按细项补测中。旧 P0 52/52 和既有真机证据继续保留，但不能替代本轮新增功能逐项证据。今晚实施目标只收口无需扫码范围；扫码后保持待测，不解锁 acceptance 4/4。

### 当前浏览器模式标准（2026-09-21）

- 无头模式使用 Playwright 真实 `headless:true`，不创建可见浏览器窗口；可视化模式使用真实 `headless:false`，创建并保持可见的浏览器窗口。
- 两种模式都使用 `channel: 'chromium'` 和该学生自己的持久化 profile。不能用 macOS 隐藏、CDP、移动窗口或同一进程动态显隐伪装另一种模式。
- 运行中模式锁定；停止并释放上下文后，才能按新模式重新启动并复用登录态。二维码不触发模式切换，统一使用程序内“查看作业二维码”。
- 旧记录中的“隐藏式无头”“打开可视化浏览器”“同一进程显隐”只用于历史追溯，不是当前实现或测试规则。

2026-09-21 继续复测：同一持久化 profile 分别执行真实无头和有头单账号提取。无头模式登录、扫 4 门课程、处理 7 份历史作业，44 次显隐采样均为不可见；有头模式同样完成登录、扫课和 7 份历史作业，50 次采样均为可见。两次结束后浏览器数均回到 0；无头复测最终新增 0、去重 1007、跳过 214、冲突 0、写入失败 0，有头复测新增 0、去重 1009、跳过 212、冲突 0、写入失败 0。显示模式测试同时确认运行中切换被拒绝，停止后 PID 重建且 profile 复用。

2026-09-21 最新单账号模式复测：无头真实提取 4 门课程、7 份作业、20 页历史，44 次显隐采样均为 false；有头真实提取同样完成 4 门课程、7 份作业、20 页历史，50 次显隐采样均为 true。两次 extract-summary-verified 均通过，浏览器数均回到 0，作业统计逐项求和一致。此前答题探针未进入 occupying_verify 的记录已被 2026-09-22 真实“做作业”二维码探针更新；无头和有头均进入 needs_verify，扫码后作答仍待用户协助。

2026-09-21 最新提取实测：目标账号配置解析为 8 个唯一账号，重复账号未重复启动。提取题库、全部课程、无头模式、账号并发 2、课程并发 2；8 个学生共完成 112 份作业，全部进入明确终态。浏览器隐藏采样 380 次全部为隐藏，运行结束浏览器数为 0；总计新增 41、去重 4631、跳过 888、冲突 0、写入失败 0。该结果只收口本轮提取模式无扫码路径，不能替代扫码后答题、提交、成绩、校对和即时回写验收。

2026-09-21 追加产品 E2E：工作台通过（4 门课程、选课高亮、0 门开始门禁、10 条历史成绩最新优先、课程日志隔离、刷新入口和头像移除）；课程选择通过（两学生独立选择、重新扫描清空旧选择、未选学生跳过、队列范围正确、停止后恢复编辑）；运行锁通过（UI/IPC/模式锁定及停止解锁）；显示模式通过（分别以真实 `headless:true`/`headless:false` 启动，运行中拒绝切换，停止后复用 profile 重新启动）；题库查看通过（3 个合法密码、冻结持久化/到期、单删、批删部分失败和回读）；题库导入通过（新增 1、去重 1、跨课程合并 1、选项乱序合并 1、冲突 1、跳过 11、写入失败 1、回读失败 1、本批清理和非本批保护）；AI 连通性真实结果为 14 个模型中 11 成功、3 失败，峰值并发 3，单模型重测和设置同步通过。

2026-09-21 代码修复：题库查看解锁后先完成课程分类读取，再加载题目列表，消除课程下拉为空的前端竞态；题库按 content_hash 读取增加一次有界重试，持续失败仍保留待回写。修复后 P0 70/70、构建通过，真实无头提取写入失败从 1 条复测为 0 条。

### TASK-B03 并发推荐、配置同步与运行锁 — REQ-031 AC-032

- [x] B03-01 复用真实 system metrics，计算保守推荐账号/课程并发。
- [x] B03-02 设置保存后主进程广播，工作台和学生卡立即同步。
- [x] B03-03 启动任务生成配置快照；运行中 UI 控件禁用。
- [x] B03-04 主进程拒绝运行中的并发修改；停止后恢复。
- [x] B03-05 显示推荐值、当前值和超建议提示，推荐不强制覆盖。
- [x] B03-06 保存/广播失败保留旧配置；运行中“恢复默认”同样不能改变并发。

2026-09-21 实证：`npm run test:e2e:settings` 真实采集本机指标、推荐值、超建议提示和连续快照，保存账号/课程并发 1 后设置页与工作台同步；非法配置保存返回失败，旧 URL/并发保持不变且没有 `settings:changed` 广播；`npm run test:e2e:run-lock` 证明运行中 UI 与主进程均锁定，开始执行后选课同样锁定，停止后恢复编辑。

### TASK-C03 工作台执行范围、历史成绩、课程日志与视觉清理 — REQ-024 REQ-025 AC-025 AC-026

- [x] C03-01 移除重复状态，只显示主状态、当前动作、进度。
- [x] C03-02 增加全部/可选课程分段选择，默认全部。
- [x] C03-03 可选模式检测后停住、通知、按学生独立选课和高亮。（node tests/e2e-course-selection.mjs 已真实覆盖通知、停住、双学生独立选择和高亮）
- [x] C03-04 汇总选择并生成队列；未选课程明确跳过，未选学生分支保留独立用例。
- [x] C03-05 运行中锁定范围、工作模式和课程选择。
- [x] C03-06 作业行增加历史成绩，真实只读历史弹窗覆盖正常历史列表。（viewable、unfinished、no_view 已真实 UI 通过；空态、未批阅和读取失败已遍历并保留受控分支，当前账号未自然触发）
- [x] C03-07 日志按当前学生+当前课程隔离，未选课程禁用。
- [x] C03-08 删除学生卡刷新和顶栏头像，重排状态区并处理长文本。
- [ ] C03-09 开始前汇总已选学生/课程/未选学生；历史按最新优先；日志覆盖规定动作和五级颜色。（历史排序已实测；多学生汇总、日志全级别仍待测）

2026-09-21 实证：`npm run test:e2e:workbench` 真实扫描 4 门课程并停在等待选课；验证单选高亮、0 门开始禁用、历史成绩 10 条最新优先、当前课程日志隔离、学生卡刷新删除、顶栏头像删除。`node tests/e2e-course-selection.mjs` 使用两个已保存真机账号连续执行两轮，验证每生独立选择、重新扫描清空上一轮选择、只给一个学生选课时另一个学生明确进入“本轮未选择课程”且提取统计清零、队列仅含选中课程、停止后恢复编辑；`node tests/e2e-run-lock.mjs` 继续证明开始后选课、模式和并发均锁定。桌面通知、历史异常分支和日志全级别仍未标绿。

### TASK-D02 窗口显隐真实性、本机状态和浏览器计数 — REQ-030 AC-031

- [x] D02-01 无头以 `headless:true`、可视化以 `headless:false` 分别启动；运行中拒绝切换，停止释放后按新模式重启并复用 profile。
- [x] D02-02 无头提取全程不创建可见窗口；UI 标签、进程启动参数与真实窗口状态一致。
- [x] D02-03 顶栏 CPU、系统内存、软件内存和压力状态接真实采样。
- [x] D02-04 浏览器数严格聚合 launching+occupied+occupying_verify，不含排队。
- [x] D02-05 创建、验证暂停、释放、崩溃后及时刷新且无窗口泄漏。
- [x] D02-06 任务结束保留 profile/登录态；采样失败显示 unavailable/error，不保留演示或陈旧值。（profile/登录态已真实复测；采样失败语义由实现、P0 和受控 RSS 缺样本分支验证，真实故障留用户现场复核）

2026-09-21 规则纠正：此前 7/8 账号提取和同一 PID 显隐证据基于 `headless:false` 加 macOS 隐藏，只能证明提取流程和旧窗口隐藏行为，不能证明真实 Playwright 无头。该旧证据对 D02-01、D02-02 和 T15-G01 至 G03 作废；已改为真实 `headless:true/headless:false`，并由 `node tests/e2e-display-toggle.mjs`、`npm run test:p0` 和构建复测证明启动参数、运行锁、停止后重启和 profile 复用成立。
2026-09-21 异常释放修复：真实杀掉无头浏览器根进程后，`node tests/e2e-display-toggle.mjs` 观察到浏览器进程消失、顶栏浏览器数归零，随后停止并重新启动可视化模式成功；池在 BrowserContext close 时清理持有表、样本和槽位，避免崩溃后排队永久等待。

### TASK-G02 程序内二维码与人工验证闸门 — REQ-026 AC-027

- [x] G02-01 定位真实二维码弹窗容器并只截取该区域。
- [x] G02-02 以内存 PNG 和脱敏上下文经 IPC 下发，不落盘、不记录日志。
- [x] G02-03 弹窗实现查看、复制图片、刷新、验证完毕和关闭。
- [x] G02-04 学生/课程/作业/任务切换使旧码失效；同生只保留一个会话。
- [x] G02-05 无头模式扫码不显示浏览器并使用程序内二维码；可视化模式保持窗口可见，同时允许浏览器扫码或程序内二维码。
- [x] G02-06 验证完毕前不继续；点击后真实检查弹窗消失和页面可继续。
- [x] G02-07 扫码前能力已由真实“做作业”触发并完成验证；真实手机扫码后的恢复留到用户协助时逐项复测。
- [x] G02-08 快照包含任务/课程/作业/时间/版本；刷新优先真实入口，无入口时安全重开验证弹窗。

### TASK-I02 题库首页、查看、密码门禁、查询删除与导入实测 — REQ-028 REQ-029 AC-029 AC-030

- [x] I02-01 题库页增加导入/查看两张卡片，不增加顶栏入口。
- [x] I02-02 主进程保存三个密码的不可逆哈希；5 次错误冻结 60 秒且重启不可绕过。
- [x] I02-03 服务端分页查询，支持课程筛选、模糊/精确搜索和清除。
- [x] I02-04 表格七列、格式化时间、长文本展开和稳定布局。
- [x] I02-05 单删/批删二次确认、逐条结果和回读确认。
- [x] I02-06 独立批次真实 Electron + Supabase 导入覆盖新增、重复、跨课程合并、选项乱序、冲突、缺字段、空值、未知题型、非法 JSON、损坏文本、写入失败和回读失败。
- [x] I02-07 只清理本批 hash 并回读为零；测试前后抽样非本批真实题目内容一致。

2026-09-21 实证：`npm run test:e2e:bank-import` 输出 added 1、duplicate 1、courseMerged 1、shuffledMerged 1、conflict 1、skipped 11、writeFailure 1、readbackFailure 1、nonBatchProtected true；写入/回读失败使用主进程受控故障，仅用于证明失败统计与 UI 不冒充成功，其余结果均来自真实 Supabase。

### TASK-J02 AI 全模型连通性诊断 — REQ-027 AC-028

- [x] J02-01 复用当前模型清单和请求客户端做真实最小请求。
- [x] J02-02 最多并发 3，逐模型状态、耗时、HTTP 和规范化失败原因。
- [x] J02-03 支持单模型重测，不改变答题降级顺序。
- [x] J02-04 按可用数量输出四级结论并显示检测时间。
- [x] J02-05 展示 pending/queued/running；限流显示 Retry-After 或实际等待时间。（queued/running 真实可见，Retry-After 由 P0 受控验证；真实限流留用户现场复核）

2026-09-21 实证：14 个模型真实检测峰值并发 3，最新一轮 11 成功、3 失败；首帧全部 queued，运行中状态逐项切换，失败模型单独重测只返回目标模型，四级汇总和检测时间均在弹窗显示。未自然出现鉴权、限流、网络、服务端失败；限流/异常分类和 Retry-After 仍保留离线证据，答题模型顺序不提前标绿。
2026-09-21 追加实证：`node tests/e2e-settings.mjs` 在真实单模型重测后断言 `AI_MODELS` 顺序保持不变，重测不会改变答题降级顺序。

2026-09-22 浏览器池与推荐实测：node --experimental-strip-types tests/e2e-browser-pool.mjs 真实启动 3 个目标账号，账号并发 2 时 2 个学生占用浏览器、1 个目标学生排队且不计入浏览器数；压力为真实快照状态，recommendationSampled=true，浏览器平均内存为真实 RSS 汇总约 963MB。此前页面 performance.memory 在 Chromium 中为 0 的问题已修复；采样失败仍保留保守回退状态。
2026-09-22 采样失败语义修复：无运行浏览器时继续显示保守估算；有运行浏览器但 RSS 尚未形成完整样本时显示 unavailable，不沿用旧样本或把回退值冒充浏览器均值。真实运行样本复测仍通过。

### TASK-T02 无扫码全功能 E2E 与扫码后分界 — CHG-025

- [ ] T02-01 将 08 和 09 每个新增功能拆成独立用例，不漏按钮、状态和异常分支。
- [x] T02-02 跑已接线的无需扫码产品 E2E、集成测试、P0、类型检查、构建和 diff 检查；普通页面仍有矩阵细项待补测。
- [x] T02-03 完成本轮敏感信息、临时数据、二维码文件和账号存储反斜杠扫描；运行中崩溃/采样失败仍保留独立待测。
- [ ] T02-04 逐项执行 09 的全部 T15 行；二维码出现后但无需实际扫码的能力不得错误延期。
本轮新增真实浏览器池证据已覆盖 T15-G09、T15-G10、T15-G12、T15-H04；T15-G21 的采样失败专门故障场景仍待测，扫码后 J 用例继续待条件。
- [x] T02-05 开始业务实现前将开发版本升至 0.1.6 并更新 CHANGELOG；构建和报告核对版本一致。
- [ ] T02-04 每项写入真实证据；失败立即回写对应 TASK 后修复复测。
- [ ] T02-05 扫码后每项保留待测原因和后续动作，不记通过/失败。

2026-09-21 实证：重新构建 0.1.6 产物后执行 7 个唯一账号无头提取复跑，104/104 作业进入终态；总计新增 0、去重 4366、跳过 861、冲突 0、写入失败 0，待回写队列为 0。前轮 9 个写入失败未复现，按本轮真实结果记录为“未复现”，不抹除前轮失败证据。
2026-09-21 追加验证：npm run test:e2e:settings、npm run test:e2e:run-lock、npm run test:e2e:workbench、npm run test:e2e:bank、npm run test:e2e:bank-import、node tests/e2e-course-selection.mjs 和 node tests/e2e-display-toggle.mjs 均退出码 0；npm run test:p0 70/70，npm run build、git diff --check -- . 通过。工作区敏感信息扫描无命中，临时测试批次和测试 profile 无残留。普通矩阵细项仍按 09 独立行继续补测，不能以集成命令替代未覆盖行。
2026-09-21 追加验证：新增保存失败旧配置保留、广播抑制和浏览器崩溃释放回归均通过；`npm run test:e2e:settings` 增加检测时间界面断言，`node tests/e2e-display-toggle.mjs` 增加真实浏览器进程终止后的名额归零断言。工作区不提交、不推送、不发布。

### 增量开发顺序

TASK-B03/D02 基础状态与配置 → TASK-C03 工作台与课程范围 → TASK-G02 二维码闸门 → TASK-J02 AI 诊断 → TASK-I02 题库能力与导入实测 → TASK-T02 全量无扫码真机。能并行的离线测试可并行，涉及共享主进程状态和真实 Supabase 的修改与测试顺序执行。

## 2026-09-20 CHG-017 每生可选重新答题次数

- 学生卡片工作模式下新增 1–10 轮选择器，按学生写入本机加密账号记录，默认 10；旧账号缺字段自动兼容为 10。
- 次数判定统一使用该生选择值：无限制取选择值；有限且上限或剩余不超过 3 仍为 1；其余取 `min(选择值, 剩余)`。
- 已补 T-K3 回归，验证默认 10、选择 2、有限作业安全上限。离线 P0 52/52、Vue 与 Electron 类型检查通过。
- 2026-09-20 产品真机以单个持久化账号、选择值 2 运行：同一份无限制作业第 1 轮 60/60 后提交并读取已批阅历史，得分 82，校对匹配 60/60，AI 正确入库 1；第 2 轮 60/60 后得分 90，校对匹配 60/60。随后该作业变为 `not_full_next_time`，未出现第 3 轮，并自动进入同课程下一份作业且轮次从 1 开始。测试进程在下一份作业作答中主动停止，持久化 profile 保留。

## 2026-09-20 CHG-016 本轮同步校对回写

- 根因：公共经济学第 7 轮校对产生待回写后，旧流程主动释放浏览器并结束学生，因此只处理前两门课程，后续五门没有继续。
- 新规则：每次提交后必须在当前运行等待本次已批阅历史并完成校对回写；失败保持该学生浏览器和名额占位原地重试。仅用户停止、崩溃或系统退出允许留下恢复记录。
- 修复：已移除正常运行的“下次先补偿并释放”分支；启动残留和本轮新增待回写都在当前会话循环补偿。已验证答案冲突成功标记后视为已处理，只有真实写入失败继续重试。
- TDD：新增“冲突已处理、失败原地重试、停止才退出”回归；当前 P0 52/52，通过。真实产品仍需复用 profile 验证回写完成后继续第 3 至第 7 门课程。

## 2026-09-20 CHG-015 稀有场景验收口径

- 用户确认：题库错答案真实删除、未作答警告、14 模型全部失败/30 轮耗尽、待回写及云端/网络失败，不要求为真机验收人工制造。自然运行中触发则必须修复并复测；完整 E2E 结束仍未出现则记录未触发且不阻塞。
- 该确认只调整真机完成门禁，不删除产品防护、状态反馈、离线测试和错误处理实现；不得把未触发写成已通过。
- 当前仍为 development 3/4，继续只跑一个已扫码账号的全部课程；第二账号未启动，持久化 profile 保留。

## 2026-09-19 逐题写入确认修复

- TASK-L01 修复跨账号待回写覆盖：保存前重新读取最新队列，只替换完成补偿的账号；新增交错更新回归，P0 22/22，构建通过。未重启当前活跃作答窗口加载此修复。
- TASK-G01 尚有实测失败：验证闸门建立前其它作业提前 submit_failed，已定位提前返回分支，待修复；本阶段仍未完成。

- TASK-J01：补充非法 AI JSON 回归，复现 null 被强制转成字符串；改为严格 JSON + 非空字符串数组，单选/判断限定一个答案，不合法继续模型降级。P0 21/21 通过。此源码修复未重启正在运行的产品会话；新构建需后续加载实测。

- 更正账号后产品会话 27925 实时复核：两账号均登录并识别 6 门课程，当前均扫码等待且保留 2 个浏览器占位。依据目标“出现需要扫码就暂停”等待人工；状态机短暂组合不一致已记录到 09，恢复后继续核查。

- 新增 tests/e2e-product.mjs，使用产品 Electron + Patchright 与真实 preload/IPC；两个账号均真实登录，各扫描 6 门课程。当前两者 occupying_verify，占位浏览器数 2，按目标要求暂停等待人工扫码。未完成提交/回写验收。
- 新增 tests/answer-writeback.mts：本地真实浏览器 DOM 集成验证逐题答案等待，不冒充真实站点通过证据。

- 仍为 development 3/4。恢复检查时没有存活的产品 Electron/Chromium；重新启动开发入口。用户报告扫码完成，当前尚无恢复后真实作答通过证据。
- TASK-H01：单选/判断必须匹配期望 index；多选按正文确定目标集合，逐次检查写入，最终完整集合匹配才进入下一题。写入失败抛错停止该份作业，不再当作 AI 失败空过。
- 验证：新增完整集合回归；P0 21/21，npm run build（含 typecheck）通过，git diff --check 通过。真实站点三题型、提交和回写仍待实测，不解锁下一目标。

> 编码前门禁：文档 1/4 与 UI 2/4 已完成，development 3/4 编码已解锁。
 > 完整流程开发目标本次按 CHG-007：实现 + 离线 P0 + 产品 Playwright E2E 必须全部通过。禁止 @电脑 / Computer Use。禁止归档。最后普通目标完成后才进入 `待用户检查 → 待归档确认`。
> 开发提示词：`liran_docs/T001-development-3-4-目标提示词.md`

## 本轮目标合同

- 基础目标数：3（documentation / development / acceptance，另插入 1 次 UI）
- 当前目标：第 3 / 4 次
- 修改前评分：94/100
- 五维评分：16 / 20 / 18 / 20 / 20
- 评分证据：`liran_docs/requirements/T001-评分记录.md`
- 评分分段：90–100 完整流程候选
- 流程等级：完整流程
- 范围上限：项目级
- 安全门禁：登录、认证、密钥，最低深度；因新项目升完整
- 项目级完整条件：满足，空仓库新建桌面产品
- 当前阶段：development
- UI 目标：启用
- UI 路线：stitch
 - 外部等待：否（UI 已回传、复修并通过用户确认）
 - 验收责任：Codex Playwright E2E 真机（禁止 @电脑）；页面样式不检查
- 页面样式检查：不检查页面样式是否符合方案
- 文档 1/4：已完成并审计通过
 - UI 2/4：用户确认没问题。四页已按 Stitch HTML 落到真实 Vue 并完成两次实例级复修。Project OpenEdu Auto Desktop / 4741787672985224870。已解锁 development 3/4。
  - development 3/4：进行中。离线 P0 9/9。09 已绿：1.1–1.3、2.1–2.3、3.1–3.3、4.2。账号 20****09 needDo 已出码并 restore 等人扫码，4.3 未勾。扫码后须新进程才会跑 answerAndSubmit。TASK-L01 仍缺 history.aspx。禁止 @电脑。不得宣布本阶段完成。
  - development 3/4：进行中。离线 P0 11/11（含学生切换溢出）。09 已绿：1.1–1.3、2.1–2.3、3.1–3.3、4.2。产品浏览器已进 `assignment/preview.aspx`（可扫码两号）；不能扫码号停在预览/需验证。已修 AI fetch 12s 超时、DOM click + 等 no-click、工作台默认两胶囊+三点溢出。5–8 与 4.1/4.3 未勾。TASK-L01 仍缺 history.aspx。禁止 @电脑。不得宣布本阶段完成。
  - development 3/4：进行中。离线 P0 11/11。09 已绿：1.1–1.3、2.1–2.3、3.1–3.3、4.2、8.2。工作台默认一颗胶囊+三点。两号按不同 homeWorkId 在作答页换份；5.1/6.2 证据未齐。4.1/4.3/8.1/8.3 未勾。TASK-L01 仍缺 history.aspx。禁止 @电脑。不得宣布本阶段完成。
  - development 3/4：进行中。离线 P0 11/11。09 已绿仍是 1.1–1.3、2.1–2.3、3.1–3.3、4.2、8.2、5.4。工作台改为只显示当前 1 人，其余进三点。提交改为第一次确定必点、第二次有空题才点（全答完无第二次弹窗不再当失败）。三号现停在需验证，等人扫码后点验证完毕再收集 5.1/6.x。TASK-L01 仍缺 history.aspx。禁止 @电脑。不得宣布本阶段完成。
 - development 3/4：进行中。离线 P0 11/11。09 已绿仍是 1.1–1.3、2.1–2.3、3.1–3.3、4.2、8.2、5.4（12/25）。工作台只显示当前 1 人 + 三点。真码在后台标签，已提到前台等人扫可扫码-1/2；扫完后点验证完毕。4.1 未过（未验证时课程并行多份点做作业）。TASK-L01 仍缺 history.aspx。禁止 @电脑。不得宣布本阶段完成。
- development 3/4：进行中。2026-09-18 修复扫码成功正文仍保留二维码容器的问题，并把同一学生未验证期间的做作业点击闸门接入 runner。离线 P0 14/14。真实产品运行中两号从需验证自动进入作答，第三号继续占位等待验证；/tmp 证据已覆盖 single/multiple/judge 的 answer 写入、提交确认和提交后历史新记录门槛，09 已绿 1.1–1.3、2.1–2.3、3.1–3.3、4.2、5.1–5.4、6.1–6.3。4.3、8.1、8.3 仍待真实场景复测；TASK-L01 仍缺 history.aspx，禁止猜 DOM。禁止 @电脑。不得宣布本阶段完成。
- development 3/4：2026-09-18 追加受限课程并发：答题阶段按 `course_parallel` 展平调度，同一账号复用一个 persistent context 和扫码闸门；新增并发自检后 P0 15/15。Node 原生 E2E 扩展名问题已修复，`KAIDA_E2E_DO=0` 登录、扫课、预览分类通过；作答型独立 E2E 因新临时档案再次需要扫码且浏览器被关闭，未计入通过。4.1、4.3、8.1、8.3 与 TASK-L01 仍未收口。禁止 @电脑。不得宣布本阶段完成。
- development 3/4：2026-09-18 已用产品 Playwright 读取并脱敏保存真实 `history.aspx` DOM，确认 `.e-q-body`、`.e-q-right`、`.e-q-wrong`、`li.e-a.checked`、`.e-a-ans .e-ans-ref .e-a-g p.checked` 的真实关系；新增 TASK-L01 校对/独立提取实现、待回写候选保存、题库错题删除和最新历史记录门禁；P0 16/16。尚未取得本轮真实提交后自动校对入库/删库的端到端证据，4.1、4.3、8.1、8.3 仍待复测。禁止 @电脑。不得宣布本阶段完成。
 - 本阶段禁止项：禁止 @电脑 / Computer Use / 用本机 Chrome 代替产品 patchright；禁止预先给 09 打勾；禁止归档、禁止宣布整体完成、禁止改 N/M、禁止擅自 commit/push；禁止把密钥写入仓库

## 开发顺序

## 2026-09-18 当前进度

- development 3/4 仍进行中，当前离线 P0 为 18/18。
- 本轮修复：已验证 extract/ai_verified 结果可升级未验证导入题；两个已验证答案冲突时保留旧答案并标记 conflict；Supabase PATCH 非 2xx 或网络异常返回 failed，交给待回写链路处理；主界面增加等待批阅、校对中、校对完成、待回写补抽事件。
- 2026-09-19 提取模式补修：提取只进入客观题且排除权重 0%，不继承答题模式的满分/次数/时间窗过滤；无历史查看链接明确跳过；未批阅、未提交、续做和无查看链接计入作业跳过统计；损坏题不进入失败补偿队列；历史实时标识带提交时间；待写入安全保存失败会停止当前学生；存在失败、检测失败课程或待补偿项时显示“题库提取结束 · 有待处理项”。离线 P0 37/37。
- 提取运行入口已与自动答题回写隔离：提取模式不刷新或消费答题模式待回写队列，只补偿自己的提取待写入队列；答题模式仍保留原回写门禁。
- 已通过：npm run test:p0、npx vue-tsc --noEmit、npx vite build、git diff --check -- .。
- 2026-09-18 21:54 已清理重复 Electron/Chromium，并以单一产品进程、账号并行 2、三个账号复测：两个账号占位且进入扫码等待，第三个持续排队；同账号首次点做作业后其它并发作业未再次点击。09 的 4.1、8.1 已通过。
- 未收口：扫码完成后的 4.3、真实提交校对 7.1–7.3、窗口只 show/hide 的 8.3。
- 本阶段禁止宣布整体完成、禁止归档、禁止 commit/push。

1. [[_M]] 页面结构契约（选择器真源，先于自动化）
2. [[_A]] 桌面壳与发布（工程能起）
3. [[_B]] 设置与本机加密（凭据）
4. [[_D]] Playwright 学生浏览器池（名额与窗口）
5. [[_C]] 主界面与颗粒状态机（订阅 slot/account）
6. [[_E]] 登录与会话
7. [[_F]] 课程检测与形考判定
8. [[_G]] 同一账号扫码闸门
9. [[_I]] 题库命中去重与云端表
10. [[_H]] 作答页题型点选
11. [[_J]] 硅基流动降级
12. [[_K]] 自动提交与连刷防空转
13. [[_L]] 已批阅校对错题删除待回写
14. TASK-T01 Playwright E2E 真机（产品浏览器打真实站点，禁止 @电脑）
15. [[GitHub-OTA]] 打包更新（可与壳并行，但业务闭环后验证）

## 活动任务看板

| 顺序 | 任务 ID | 模块/任务 | PRD 需求文档 | 状态 | 实测清单 | 阻塞/下一步 |
|---|---|---|---|---|---|---|
| 1 | TASK-M01 | 页面结构契约 | `liran_docs/requirements/T001-开大自动答题桌面端-PRD.md` | 内部测试通过 | [[09-真机实测]] | 选择器在 electron/core/selectors.ts；history.aspx 仍缺 |
| 2 | TASK-A01 | Electron 工程骨架 | 同上 | 内部测试通过 | [[09-真机实测]] | patchright 捆绑 Chromium；npm run dev 已开窗 |
| 3 | TASK-A02 | GitHub OTA | 同上 | 内部测试通过（仅配置） | [[09-真机实测]] | electron-builder.yml 已配；本阶段不发 Release |
| 4 | TASK-B01 | safeStorage 凭据 | 同上 | 内部测试通过 | [[09-真机实测]] | 加密失败拒明文；禁 service role |
| 5 | TASK-B02 | 设置页与账号列表 | 同上 | 内部测试通过 | [[09-真机实测]] | Excel/手工添加已接线 |
| 6 | TASK-D01 | 浏览器池与占位 | 同上 | 内部测试及 8.1 真机通过 | [[09-真机实测]] | 单实例下并行 2 放 3 号；扫码账号继续占位，第三号保持排队 |
| 7 | TASK-C01 | 主界面布局 | 同上 | 内部测试通过 | [[09-真机实测]] | 已接 snapshot；未重做壳 |
| 8 | TASK-C02 | 五层状态机 | 同上 | 内部测试通过 | [[09-真机实测]] | 枚举只来自 PRD 第 9 节 |
| 9 | TASK-E01 | IAM 登录 | 同上 | 内部测试通过 | [[09-真机实测]] | patchright 过瑞数后进 #tab-courseList；课表 API JSON 200 |
| 10 | TASK-F01 | 检测与形考判定 | 同上 | 内部测试通过 | [[09-真机实测]] | 20****09：9 门课、24 份预览、23 份满分跳过、1 份 needDo；主观题实践未进预览 |
| 11 | TASK-G01 | 扫码闸门 | 同上 | 内部测试通过 | [[09-真机实测]] | 检测 #dl_qrCodeCheck / 标题与扫码成功正文；同号未验证点击闸门已接入；全量 HTML 未补但不猜 |
| 12 | TASK-I01 | 题库表与命中 | 同上 | 内部测试通过 | [[09-真机实测]] | hash 打乱命中、跨课 course_names P0 已绿 |
| 13 | TASK-H01 | 作答点选 | 同上 | 内部测试通过 | [[09-真机实测]] | 点 li.e-a；不认 notdo；needDo 已点做作业出码，未进 assignment/preview.aspx |
| 13 | TASK-H01 | 作答点选 | 同上 | 内部测试通过 | [[09-真机实测]] | DOM click + 等 no-click；真实 single/multiple/judge 均写入 answer |
| 14 | TASK-J01 | 硅基流动降级 | 同上 | 内部测试通过 | [[09-真机实测]] | 只认 option_texts；P0 已绿 |
| 15 | TASK-K01 | 提交与连刷 | 同上 | 内部测试通过 | [[09-真机实测]] | ok 不是 cancel；全答完无第二次确认时按成功路径；历史新记录门槛和次数闸门 P0 已绿 |
| 16 | TASK-L01 | 校对入库待回写 | 同上 | 内部实现通过，真实链路待复测 | [[09-真机实测]] | 已补 history.aspx 结构；AI 对入库、题库答错删除、批阅超时待回写已接入，尚缺提交后真实回写证据 |
| 17 | TASK-T01 | Playwright E2E 真机 | 同上 | 进行中 | [[09-真机实测]] | 1.1–1.3、2.1–2.3、3.1–3.3、4.2 已绿；4.3 等人扫码；扫码后新进程才作答提交；5–8 未勾；禁止 @电脑 |
| 17 | TASK-T01 | Playwright E2E 真机 | 同上 | 进行中 | [[09-真机实测]] | 1.1–1.3、2.1–2.3、3.1–3.3、4.2 已绿；两号已进作答页，5.x 待提交/计数证据；仅状态号保持需验证；禁止 @电脑 |

## 微观任务总表

每条含编号、REQ/AC、影响文件、依赖、验证、完成条件。当前已有 src/electron/tests，影响文件以仓库现有路径为准。

### TASK-A01 Electron 工程骨架 — REQ-001 AC-001

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-A01-1 | 建 Electron+Vite+TS+Vue3 骨架，主进程/渲染进程分离 | 无 | package.json、electron 主文件、renderer | 能启动空窗 | 仓库可 npm 启动，无扩展拷贝 |
| MT-A01-2 | Playwright 放主进程并捆绑 Chromium | MT-A01-1 | 主进程 browser 模块 | 不依赖本机 Chrome | 安装说明写明捆绑 |

### TASK-A02 GitHub OTA — REQ-019 AC-019

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-A02-1 | electron-updater 指向当前 GitHub Releases | MT-A01-1 | 打包配置 | 配置含 owner/repo | mac/Win 分平台包名正确 |
| MT-A02-2 | 发布不含密钥与 Playwright profile | MT-A02-1 | 打包 ignore | 包内无 Login Data/Key | 对照全局发布协议 |

### TASK-B01 safeStorage — REQ-002 AC-002

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-B01-1 | 账密、硅基流动 Key、Supabase URL/anon 用 safeStorage | MT-A01-1 | 主进程 secrets | 重启仍可读；仓库搜不到 Key | 加密失败拒绝写明文 |
| MT-B01-2 | 禁止 service role 进入客户端 | MT-B01-1 | 设置与 supabase 客户端 | 代码无 sb_secret | 只有 anon |

### TASK-B02 设置页与账号列表 — REQ-004 AC-004

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-B02-1 | 设置页字段：Key、URL、anon、并行默认 2、浏览器显示默认关、日志默认开 | UI 壳 | 设置页 Vue | 对照 PRD 第 10 节 | 字段齐全 |
| MT-B02-2 | Excel 三列导入姓名账号密码，跳过表头 | MT-B02-1 | 导入逻辑 | 导入后卡片出现 | 不把样本学号当默认数据 |
| MT-B02-3 | 删除账号释放浏览器名额；占用中刷新先停再检测 | [[_D]] | 卡片操作 | 不并行再开一套做作业 | AC-004 |

### TASK-D01 浏览器池 — REQ-005 REQ-021 AC-005 AC-021

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-D01-1 | 账号并行 N：同时最多 N 个学生浏览器 | MT-A01-2 | slot 池 | 第三人排队 | 顶栏=launching+occupied+occupying_verify |
| MT-D01-2 | 一生一 launchPersistentContext，同号多份作业共用 | MT-D01-1 | context 管理 | 两份预览同 Context | 禁止每份一浏览器 |
| MT-D01-3 | 扫码暂停继续占名额，禁止给排队学生补位 | MT-D01-1 | slot 状态机 | 暂停时仍 2 窗、第三人仍排队 | 进程不关 |
| MT-D01-4 | 真实无头/可视化：分别以 headless:true / false 启动；运行中锁定 | MT-D01-1 | 启动参数与进程检查 | 无头无窗口、可视化有窗口 | 停止释放后切换并复用 profile |

### TASK-C01 主界面布局 — REQ-003 AC-003 AC-023

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-C01-1 | 顶栏 CPU/内存/软件占用/浏览器数/压力；不放切换按钮 | UI 壳 | 顶栏组件 | 浏览器数不含排队 | 采集失败显示不可用 |
| MT-C01-2 | 左列科目、右列作业态、右侧学生卡片、底栏进度、主按钮「登录并刷新课程」 | UI 壳 | 主界面 | 不内嵌完整浏览器 | 比参考图精致，样本姓名不入库 |
| MT-C01-3 | 始终显示题库答题 x / AI 答题 y；卡片主句=账号态+当前动作 | [[五层状态机]] | 卡片/日志 | 不能只写进行中 | AC-003 |

### TASK-C02 五层状态机 — REQ-022 AC-022

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-C02-1 | 主进程五层英文枚举，渲染只订阅 | MT-D01-1 | state 模块 | 不得另造同义码 | 对照 PRD 第 9 节 |
| MT-C02-2 | 每次点击/跳转/弹窗推一条进度 | MT-C02-1 | 日志 | 字段不含密钥 | 例句能覆盖登录到等批阅 |
| MT-C02-3 | 崩溃只 released 该号，已占名额其它号继续 | MT-C02-1 | 池 | 排队不补暂停名额 | |

### TASK-E01 IAM 登录 — REQ-006 AC-006

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-E01-1 | 只读搬改上号器 content.js 填账密点登录，不拷扩展 | MT-D01-2 | 登录模块 | 正确账密到 scenter | 本机路径缺失则受阻 |
| MT-E01-2 | 错密停该号其它号继续；IAM 验证码走同一套暂停 UI | MT-E01-1 | 登录错误处理 | 连续失败停号 | 登录页 DOM 开发前补结构 |

### TASK-F01 检测与判定 — REQ-007 REQ-008 REQ-009 AC-007 AC-008 AC-009

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-F01-1 | 确认 #tab-courseList，只取 #pane-courseList .course-item | MT-E01-1 | 扫课 | 收藏课不出现 | |
| MT-F01-2 | 点 #courseHomeWorkNew，只读两块；暂无数据=无作业 | MT-F01-1 | 形考列表 | 不点实践块 | |
| MT-F01-3 | 客观题权重>0% 进预览；0% 与非客观跳过；100 分已批阅跳过；不点续做 | MT-F01-2 | 预览判定 | 次数解析无限制/-1 与有限 | |
| MT-F01-4 | 主按钮只跑检测；答题模式检测完自动开答；提取模式不开答 | MT-F01-3 | 工作流 | 检测阶段不点做作业 | |

### TASK-G01 扫码闸门 — REQ-010 AC-010

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-G01-1 | 未验证同时只允许一份点做作业 | MT-F01-4 | 闸门 | 三份并行只一张码 | 旧弹窗取消后刷新 |
| MT-G01-2 | 无头弹码不显示浏览器，只提供程序内二维码；可视化窗口保持可见并同时提供程序内二维码 | MT-D01-4 | 卡片按钮 | 不新开浏览器、不切换模式 | |
| MT-G01-3 | 验证完毕先由主进程复核页面成功态，再刷新其余预览并继续；不改变浏览器模式 | MT-G01-2 | 闸门 | 约 2 小时以页面是否再弹码为准 | 扫码弹窗 HTML 待补 |

### TASK-I01 题库 — REQ-012 REQ-018 AC-012 AC-018

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-I01-1 | content_hash=题型+规范化题干+排序后选项集合；course_names 追加去重不进 hash | 无 | hash 工具；questions 表 | 顺序打乱仍同一 hash；跨课仍一行 | 不用 ABCD；禁止按课拆表 |
| MT-I01-2 | JSON 导入去重；不提供导出 | MT-I01-1 | 导入页 | 两条仅顺序不同合并 | |
| MT-I01-3 | 独立提取只抽已批阅；自动提取随答题校对 | MT-I01-1 | 提取模式 | 提取不点做作业 | |
| MT-I01-4 | 独立提取按作业并发读取全部可查看历史；不看分数，只提取正确题 | MT-I01-3 | 提取模式、浏览器池、并发调度 | 100 分和非 100 分作业都进入历史提取；不点做作业、不提交 | |
| MT-I01-5 | 每份作业记录新增、去重、跳过、冲突、写入失败五项统计，并汇总到账号/本次运行总计 | MT-I01-4 | 实时状态、题库写入、主界面 | 作业明细五项之和等于运行总计；统计来自真实结果，不使用固定演示值 | |
| MT-I01-6 | 提取模式不按分数、次数或时间窗过滤；范围内作业全部进入历史读取 | MT-I01-4 | 课程检测、提取模式 | 100 分、非 100 分、低分及当前不可作答作业均不因答题规则被漏掉；主观题、实践、资源学习和 0% 权重仍跳过 | |
| MT-I01-7 | 多层提取并发受限 | MT-I01-4 | 浏览器池、课程并发、历史任务调度 | 多学生、单学生多课程、单课程多作业历史可并发；一生一 Context，历史任务只开 Page | |
| MT-I01-8 | 历史记录筛选与只读边界 | MT-I01-4 | history 读取 | 只打开已批阅且有查看链接；不点做作业、续做、提交、不调用 AI | |
| MT-I01-9 | 正确题解析与损坏题保护 | MT-I01-4 | history 解析、题库写入 | 只写明确正确且字段完整题；错误、未知、无法判定、空字段、替换字符进入跳过/失败统计 | |
| MT-I01-10 | 提取失败隔离和完成门禁 | MT-I01-7 | 错误处理、实时状态 | 单作业失败继续其它任务；结果不丢；学生和本次运行都要等全部范围处理完并记录失败项后再结束 | |

### TASK-H01 作答点选 — REQ-011 AC-011

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-H01-1 | 单选点一个正文，[name=answer] 与 a.e-item.active | [[按正文回填]] | 作答页操作 | 对照开大自动答题页面结构.md | |
| MT-H01-2 | 多选点齐全部正确正文 | MT-H01-1 | 作答页操作 | 少点不算 selected | |
| MT-H01-3 | 判断按正确/错误正文；页面 index 1=正确 0=错误仅作对照 | MT-H01-1 | 作答页操作 | 不以字母为准 | 未知题型阻止提交 |
| MT-H01-4 | 作答前整卷文本预检；提交前隐藏答案与右侧 active 双检；漏题最多补答 30 轮 | MT-H01-1 | answer/page-tools | 第 1–29 轮继续；第 30 轮失败安全停止且未提交 | 每轮重建最新复核计划并实时显示 |

### TASK-J01 AI — REQ-013 AC-013

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-J01-1 | 14 模型按去重顺序；统一高校课程客观题提示词；只认 {"option_texts":[...]} | 设置 Key | AI 客户端 | 非法 JSON 换下一个，模型 ID 无重复 | 设置页可换 Key |
| MT-J01-2 | 单轮全失败显示题号、尝试数与最终原因；最多补答 30 轮 | MT-J01-1 | 作答循环/主界面状态 | 每轮实时显示；第 30 轮失败显示剩余题号和未提交 | 禁止静默结束和漏题卷 |

### TASK-K01 提交连刷 — REQ-014 REQ-015 AC-014 AC-015

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-K01-1 | 整卷双检通过后点 #submitHomeWork；正常 xcConfirm 点深蓝 ok；未作答警告点绿色 cancel | MT-H01-4 | 提交 | 漏题警告不得点确定 | 成功=历史有效新记录 |
| MT-K01-2 | 约 8 秒无弹窗再点一次提交；仍无则失败不扣次数当次停 | MT-K01-1 | 提交 | | |
| MT-K01-3 | 每生可选 1–10（默认 10）；无限制取选择值；≤3 则 1；其余 min(选择值,剩余)；到 100 停 | MT-K01-1 | 连刷 | | |
| MT-K01-4 | 必须已批阅并校对完才允许新开；空转提前停 | [[待回写补抽]] | 连刷闸门 | 批阅超时当次停 | |

### TASK-L01 校对待回写 — REQ-016 REQ-017 AC-016 AC-017

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-L01-1 | 已批阅查看：AI 对的入库，错的不入库 | MT-K01-1 | 校对 | history.aspx 开发前补 DOM | |
| MT-L01-2 | 题库答错删除该 hash，下一轮走 AI | MT-L01-1 | 删库 | | |
| MT-L01-3 | 批阅超时/查看失败 → 待回写，当次不得新开；下次该号先补抽补删 | MT-L01-1 | 队列 | | |

### TASK-T01 Playwright E2E — REQ-023 AC-024

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-T01-1 | 用产品 Playwright 打真实开大站点，按 09 逐步跑 | TASK-A01 至 TASK-L01 | tests 或主进程 e2e | 09 全绿 | 禁止 @电脑 |
| MT-T01-2 | 扫码等人手后必须继续；缺 DOM 回写受阻不得猜 | MT-T01-1 | 04/09 | 人工步骤以外全自动 | |

### TASK-M01 页面结构 — REQ-020 AC-020

| 编号 | 内容 | 依赖 | 预定影响文件 | 验证 | 完成条件 |
|---|---|---|---|---|---|
| MT-M01-1 | 自动化只读 docs/page-structures 与根作答页结构 | 无 | 选择器模块 | 两条 preview 路径不混 | |
| MT-M01-2 | 登录页全量 DOM、扫码弹窗 HTML、门户顶栏学生信息、history.aspx 开发到该步带用户补，禁止先猜 | 用户配合 | docs/page-structures 新文件 | 未补前无抽取代码 | |

## 状态迁移规则

1. `待开发 → 进行中`：开始开发。
2. `进行中 → 内部测试通过`：内部验证通过。
3. Playwright E2E 分支：`内部测试通过 → 待实测 → 真机测试通过`，再交 4/4 做问题回写与用户检查交接。禁止 @电脑。
4. 用户没有明确反馈通过前，不得标记已完成。
5. 本目标是 development 3/4。UI 壳已完成。业务按开发顺序实现后必须跑通 09 Playwright E2E。扫码等人手。不得宣布整体完成、不得归档。

## 循环依赖告警

## T002 开发与验收目标 2/2（进行中）

状态：正式 PRD 已确认；第 1/2 次文档、任务拆分和验收准备已完成。本轮完成实现、内部验证、扫码前测试边界回写和发布准备后，进入待用户检查、待归档确认。版本目标 2.2.0，仅推送 GitHub。

| 任务 | 需求/验收 | 入口与范围 | 本轮证据/下一步 |
| --- | --- | --- | --- |
| T002-01 | REQ-001 / AC-001 | `electron/answer.ts` 保留 `locator.click()` 首选，逐项等待保存；失败按 `checked` DOM click 降级；最终核对答案、勾选和右侧已做 | 多选延迟保存回归已通过；第 2/2 次复核超时和不提交边界 |
| T002-02 | REQ-002 / AC-002 | `electron/core/ai-rate-limit.ts` 全局间隔 750→1000ms，保留 429/Retry-After 和模型顺序 | P0 节流用例已通过；第 2/2 次复测请求间隔和耗时 |
| T002-03 | REQ-003 / AC-003 | `runner.ts`、IPC 和 `HomeView.vue` 实现四项快照、学生级锁和唯一批量按钮 | T002 UI 回归已通过；第 2/2 次审查部分失败和锁绕过 |
| T002-04 | REQ-004 / AC-004 | `AccountsView.vue` 二次确认，主进程重核账号集合、停止释放后逐项删除 | T002 UI 回归已通过；第 2/2 次复测集合变化和运行排队 |
| T002-05 | REQ-005 / AC-005 | 课程模式并入批量按钮，课程勾选按学生隔离 | 离线配置回归已通过；第 2/2 次复核可选课程等待 |
| T002-06 | REQ-006 / AC-006 | 学生卡独立开始，停止等待旧资源释放后重新扫描并入队 | 离线调度回归已通过；第 2/2 次复核快速停止/开始 |
| T002-07 | REQ-007 / AC-007 | 手动、Excel、粘贴成功导入共用现有调度，空位运行、满额排队、释放补位 | 离线调度和 UI 回归已通过；第 2/2 次复核全部停止竞态 |

内部证据：`npm run test:p0`、`node tests/multiple-answer.mjs`、`node tests/t002-ui.mjs`、`node tests/t002-scheduler.mjs`、`npm run test:t002-post-scan` 和 `npm run build` 均需在本轮最终复测通过。扫码前真实学校站点因待用户凭据保持待执行；扫码后只记录内部模拟通过，不标记为真机扫码后通过。Windows 真机、正式 OTA 安装重启和用户验收仍待后续检查，不得直接归档。
