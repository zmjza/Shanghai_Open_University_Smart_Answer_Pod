# 测试用例

## 2026-09-24 T001 归档收口

用户确认验收完毕；T15-J01～J18、CHG-026 U01-01～U01-08 已按当前验收口径收口。详细证据保留在 `liran_docs/09-真机实测.md`，归档包为 `liran_docs/archive/T001-开大自动答题桌面端/归档.md`。

## 2026-09-22 扫码后双并发实测回写

- 两个学生均完成真实手机扫码，并在产品页面分别点击“验证完毕”；验证前均保持 needs_verify/occupying_verify，验证后才继续。
- 可视化答题已取得真实题目读取、题库/AI 回填、提交、分数和历史校对证据；无头双并发复用持久化 profile，最终 2/2 学生为 round_ended，浏览器数为 0。
- 依据用户既有真机结果，T15-J02、J03、J04、J06、J07、J17 通过；J12、J13、J16 按“运行至结束未自然出现则默认通过”口径通过并保留备注。
- T15-J11 已完成扫码后独立复测：两个学生均先在真实工作台等待选课，再通过学生菜单和课程卡片各自选择 1 门课程，页面显示“开始执行（已选 2 门）”后点击真实开始按钮；运行只遍历所选课程，最终双学生 `round_ended`、`running=false`、`browsers=0`。J 全部收口后执行 T15-A～J 串行全链路回归。
- 本轮验证命令：`npm run test:p0`（70/70）、`npm run build`、`node --check tests/e2e-product.mjs` 均退出码 0。

2026-09-20 CHG-017 真机证据：学生选择 2 轮后，同一份无限制作业仅执行两轮；两轮均完成 60/60、提交、历史校对和即时回写，第 2 轮未满分后标记下次再做，没有第 3 轮；随后进入同课程下一份作业并从第 1 轮重新计数。

2026-09-20 CHG-016 增补：每轮提交后必须在当前运行等待本次已批阅历史并完成题库回写，非满分也不得跳过；回写暂时失败时保持浏览器占位原地重试，禁止释放后写“下次补偿”。冲突成功标记视为已处理，只有真实写入失败重试。离线 P0 52/52，真实全部课程续跑待验证。

2026-09-20 CHG-015 验收口径：题库错答案删库、未作答警告、全模型失败/30 轮耗尽、待回写及云端/网络失败保留离线回归；真实自然运行若触发必须修复并复测，完整 E2E 结束仍未触发则记录未出现且不阻塞，不做破坏性故障注入，不冒充真机通过。

2026-09-20 CHG-013/014 增补：AI 全局请求启动间隔调整为 0.75 秒，429 仍至少冷却 5 秒；题库成功回填后随机等待 1–3 秒，普通作答和补答共用同一规则。批阅列表缺总分时按每题 `GiveScore` 求和，新一轮保留并标明上次分数。离线 P0 50/50 通过，真实站点跨作业和跨课程收口仍在运行。

2026-09-19 T-J1 增补：拒绝 null、数字、空字符串和夹带说明的 JSON；重复正文去重。先复现失败再修复，P0 21/21 通过。

2026-09-19 补充：答案集合完整匹配回归覆盖漏选、多选、旧答案、判断值 0 和选项顺序；P0 21/21、npm run build 通过。仅为离线证据，不能替代 09 的真实运行。

最后普通目标的“完成”和项目的“已完成”必须分开记录：普通目标完成后立即回收并写入 goal_status=reclaimed，项目保持待用户检查，资料保持待归档确认。只有独立 archive 成功后，项目才允许进入已归档 → 已完成；archive 不计入普通目标数量。

> 各模块/交互点的内部验收用例。真机实测见 `09-真机实测.md`。
> 本文件只保留活动任务用例和精简稳定回归基线。

## 当前浏览器模式基线

T-D3/T-D4/T-D5 和 T-G2/T-G3 均按真实启动模式执行：无头为 Playwright `headless:true` 且无可见浏览器窗口；可视化为 `headless:false` 且窗口全程可见。运行中拒绝切换，停止后重新启动并复用 profile。二维码不显示浏览器、不改变模式，统一验证程序内二维码弹窗。旧“打开可视化浏览器”和“验证后自动藏窗”只属于历史用例记录，不作为当前验收标准。

## 已归档任务基线

- 任务 ID：T001
- PRD 需求文档：`liran_docs/requirements/T001-开大自动答题桌面端-PRD.md`
- 归档前状态：离线 P0 与真实产品证据已记录；T001 已于 2026-09-24 完成用户验收并归档。禁止 @电脑。

2026-09-21 最新无头提取产品证据：目标文件中的 9 条账号配置解析后得到 8 个唯一账号；重复账号未重复启动。8 个学生共完成 112 份作业，所有作业进入明确终态；运行结束浏览器数为 0，380 次窗口显隐采样均为隐藏。总计新增 41、去重 4631、跳过 888、冲突 0、写入失败 0。该证据只覆盖提取模式无扫码路径，不替代答题、扫码后恢复、提交、成绩和即时回写用例。

2026-09-21 追加产品 E2E 证据：workbench、settings、run-lock、course-selection、display-toggle、bank、bank-import 均在复测后退出码 0；AI 连通性本轮真实结果为 11 成功、3 失败，失败原因为超时或返回内容为空。题库查看脚本必须通过标准输入提供三个门禁密码；本批测试数据已清理并回读为零。

2026-09-22 追加工作台真机证据：`node --experimental-strip-types tests/e2e-workbench.mjs` 以两个已保存账号串行验证学生切换、两门课程切换、当前动作与主状态分离、题库/AI 计数、提取课程/作业进度和新增/去重/跳过/冲突/失败五项统计；修改后先红后绿，重新构建后通过。无头/有头各一账号真实提取均完成 4 门课程、7 份作业、20 页历史，分别保持隐藏/可见并正常释放浏览器。

## 用例：桌面壳 —— [[_A]]

## T002 当前目标证据（第 2/2 次开发验收进行中）

| 编号 | 场景 | 命令 | 状态 |
| --- | --- | --- | --- |
| T002-A1 | 多选正常顺序、首选失败降级、错选纠正和结果未知停止 | `node tests/multiple-answer.mjs` | 已通过 |
| T002-A2 | 全局 AI 间隔和 429 冷却 | `npm run test:p0` | 已通过，75/75 |
| T002-A3 | 四项配置同步、学生锁、课程模式隔离、全部删除确认；零账号主按钮可点并显示真实错误 | `node tests/t002-ui.mjs` | 已通过 |
| T002-A4 | 新增入队、满额排队、释放补位、单人重启、全部停止与释放后主按钮解锁 | `node tests/t002-scheduler.mjs` | 已通过 |
| T002-A5 | 类型检查、Vue 检查、Vite/Electron 应用构建 | `npm run build` | 已通过 |
| T002-A6 | 真实 runner 的验证闸门、流程推进、重启停止及资源释放；作答/提交/批阅模块为固定替身 | `npm run test:t002-post-scan` | 已通过联动模拟；不能单独证明真实模块端到端接通 |
| T002-A7 | 真实作答模块处理单选、判断、多选整卷；答案和答题卡一致 | `node tests/t002-answer-page.mjs` | 已通过受控页面与保存接口 |
| T002-A8 | 真实 AI 模块的模型降级、全失败、全局请求间隔和 429 冷却 | `node tests/t002-ai-flow.mjs` | 已通过受控网络响应 |
| T002-A9 | 真实提交模块的确认及未作答警告取消 | `node tests/t002-submit-flow.mjs` | 已通过受控页面 |
| T002-A10 | 真实批阅及题库回写：AI 正确入库、错题删除回读、假成功保留待处理 | `node tests/t002-review-writeback.mjs` | 已通过受控接口 |
| T002-A11 | 真实 runner 串联真实作答、AI、提交、批阅与题库回写，验证扫码闸门、答案保存、最新历史、完成和资源释放 | `node tests/t002-integrated-flow.mjs` | 已通过受控端到端；学校站点/浏览器占位和 AI 响应为确定性边界 |

第 2/2 次已重复上述基线，并补充扫码后分层及 A11 受控端到端验证。A6 的作答/提交/批阅使用固定替身；A7～A10 分别运行真实模块；A11 在同一次运行串联真实 runner、作答、AI、提交、批阅和题库回写，但外部学校页面与服务接口仍为本地替身。课程同步同模式误清选课先写失败断言再修复；主按钮全部释放后可重新点击，并在零账号时反馈真实错误。扫码前真实站点待凭据；内部测试不能替代真实站点、Windows 真机或正式 OTA 验收。

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 类型 |
|------|---------|---------|---------|------|
| T-A1 | 空仓库骨架已建 | 启动 App | 主窗口出现，非插件 | 正常 |
| T-A2 | 打包配置存在 | 检查依赖 | patchright 在主进程，含捆绑 Chromium | 正常 |
| T-A3 | 仓库 | 搜索扩展目录 | 无自动上号器整份拷贝 | 异常 |

## 用例：凭据 —— [[_B]]

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 类型 |
|------|---------|---------|---------|------|
| T-B1 | safeStorage 可用 | 填 Key 重启 | 仍能读出；仓库无明文 | 正常 |
| T-B2 | 加密不可用 | 保存 | 拒绝写明文并提示 | 异常 |
| T-B3 | 三列 Excel | 导入 | 跳过表头，卡片出现账号 | 正常 |

## 用例：浏览器池 —— [[_D]]

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 类型 |
|------|---------|---------|---------|------|
| T-D1 | 并行=2，3 个号 | 启动检测 | 仅 2 个学生浏览器，第三人排队 | 正常 |
| T-D2 | 一人扫码暂停 | 观察名额 | 仍占位，第三人仍排队 | 边界 |
| T-D3 | 默认无头 | 以 `headless:true` 启动 | 无浏览器窗口；运行中切换到可视化被拒绝 | ✅ |
| T-D4 | 可视化模式 | 停止后以 `headless:false` 重新启动 | 真实浏览器窗口可见；运行中切回无头被拒绝 | ✅ |
| T-D5 | 模式重启与登录态 | 停止无头后启动可视化，再启动无头 | 复用学生 profile，登录态保留；不得用同一上下文动态切换 | ✅ |

2026-09-22：node --experimental-strip-types tests/e2e-display-toggle.mjs 已验证真实 headless:true/headless:false、运行中模式锁、停止后重启和 profile 复用；tests/e2e-browser-pool.mjs 已验证账号并发 2、排队不计浏览器数和真实浏览器 RSS 推荐样本。

## 用例：检测判定 —— [[_F]]

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 类型 |
|------|---------|---------|---------|------|
| T-F1 | 已登录门户 | 扫课 | 只统计我的课程，忽略收藏 | 正常 |
| T-F2 | 两块暂无数据 | 判定 | 无作业 | 边界 |
| T-F3 | 客观题 0% | 判定 | 跳过 | 边界 |
| T-F4 | 已批阅 100 | 预览 | 跳过且不点续做 | 正常 |
| T-F5 | 点主按钮 | 检测全程 | 不点做作业 | 正常 |

## 用例：扫码闸门 —— [[_G]]

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 类型 |
|------|---------|---------|---------|------|
| T-G1 | 同号三份作业 | 并行开答 | 只一份点做作业，其余闸门排队 | 正常 |
| T-G2 | 无头弹码 | 查看作业二维码 | 浏览器保持不可见；程序内二维码弹窗可查看、复制、关闭 | 正常 |
| T-G3 | 验证完毕 | 用户完成扫码后点击验证完毕 | 主进程复核真实页面状态，成功后恢复队列；真实扫码后的恢复待扫码条件 | 边界 |

## 用例：作答 —— [[_H]] [[_I]] [[_J]]

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 类型 |
|------|---------|---------|---------|------|
| T-H1 | 单选 | 点正文 | name=answer 且 e-item.active | 正常 |
| T-H2 | 多选两正确答案 | 只点一个 | 未 selected | 边界 |
| T-H3 | 判断 | 按正确/错误正文 | 不靠字母 | 正常 |
| T-H4 | 整卷题干或选项为空/含替换字符 | 开始作答 | 最多重载两次；仍损坏则未点击任何答案并停止该份 | 异常 |
| T-H5 | 某题隐藏答案为空 | 提交前复核 | 定位题号并补答，不点击提交 | 边界 |
| T-H6 | 隐藏答案非空但右侧题号未 active | 提交前复核 | 仍判漏题并重新点击填充 | 边界 |
| T-H7 | 多选少选、多选或残留旧选项 | 提交前复核 | 与预期完整集合不一致，阻止提交 | 边界 |
| T-H8 | 补答成功 | 重建复核计划 | 最终计划使用补答后的最新正文和 index | 正常 |
| T-H9 | 漏题连续补答失败 | 提交前复核 | 第 1–29 轮继续且实时显示轮次；第 30 轮仍失败时列出剩余题号和原因，安全停止且历史无新增 | 异常 |
| T-I1 | 选项顺序打乱 | 查库 | 仍命中并点中该正文 | 正常 |
| T-I2 | 同一题出现在两门课 | upsert | 仍一行，course_names 含两课名 | 正常 |
| T-I3 | 连续题库命中或补答命中题库 | 成功回填并确认已选 | 每题随机等待 1–3 秒再进入下一题，不跳过写入确认 | 边界 |
| T-J1 | 第一模型非法 JSON | 未命中题 | 换第二模型 | 异常 |
| T-J2 | 14 模型全部失败 | 该题 | 按去重顺序全部尝试；主界面显示题号、14/14 和最终原因；最多补答 30 轮，耗尽后停止且不提交 | 异常 |
| T-J3 | 多账号同时需要 AI | 并发请求 | 所有账号共享最小 0.75 秒请求启动间隔；429 按 Retry-After 或至少 5 秒冷却后继续降级 | 边界 |

## 用例：提交校对 —— [[_K]] [[_L]]

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 类型 |
|------|---------|---------|---------|------|
| T-K1 | 整卷复核通过 | 提交 | 正常不可修改确认点深蓝确定 | 正常 |
| T-K2 | 弹窗不出 | 约 8 秒 | 再点一次；仍无则不扣次数当次停 | 异常 |
| T-K3 | 无限制未满分；学生选择 2 或 10 轮 | 连刷 | 不超过该生所选轮数，100 立刻停；未校对不得新开；旧账号默认 10 | 边界 |
| T-K4 | 站点弹“部分题目没有作答” | 提交确认 | 点绿色取消，历史不得新增，返回补答或停止该份 | 异常 |
| T-L1 | AI 题已批阅对 | 查看 | 入库 | 正常 |
| T-L2 | 题库答错 | 查看 | 删 hash | 正常 |
| T-L3 | 批阅超时 | 该份 | 待回写，当次不新开 | 异常 |
| T-I4 | 独立提取不看分数 | 100 分、非 100 分、低分历史 | 全部已批阅且有“查看”的记录都进入提取 | 只提取满分卷或漏掉非满分卷 | 正常 |
| T-I5 | 提取统计按作业记录 | 一份作业含新增、重复、跳过、冲突、失败 | 作业卡片五项统计与真实处理结果一致 | 只有总数、作业明细丢失或使用固定数字 | 正常 |
| T-I6 | 提取统计汇总 | 多课程、多作业并发提取 | 总计逐项等于所有作业明细之和 | 总计漏算、重复计算或与明细不一致 | 正常 |
| T-I7 | 提取范围独立于答题判定 | 满分、低分、次数用尽、窗口外和非当前可答作业 | 客观题作业都进入历史检查；主观题、实践、资源学习、0% 权重跳过 | 沿用答题模式过滤导致漏提 | 正常 |
| T-I8 | 提取并发隔离 | 多学生、多课程、多作业历史 | 并发受账号/课程设置限制；一生一 Context，多历史一 Context 多 Page | 新开多个浏览器或跨学生串页 | 正常 |
| T-I9 | 历史记录只读筛选 | 已批阅查看、未批阅、未提交、未完成、续做、无链接 | 只打开已批阅且有查看链接；其它记录计入跳过原因 | 点续做、点做作业或提交 | 正常 |
| T-I10 | 正确题和损坏题 | 正确、错误、未知题型、无法判断、空字段、替换字符 | 只写明确正确且字段完整题，其它不入库并有统计 | 错题或损坏题进入题库 | 正常 |
| T-I11 | 单作业失败隔离 | 一份历史页失败、Supabase 写入失败 | 失败原因可见，其它作业和账号继续，学生结束前保留失败项 | 整个学生提前成功或丢失已写入结果 | 正常 |
| T-I12 | 提取完成门禁 | 多课程、多作业、部分失败 | 全部范围扫描和历史处理结束、统计完整后才显示学生/运行完成 | 仍有任务时提前显示完成 | 正常 |
| T-I13 | 提取边界过滤 | 非客观题、权重 0%、满分、次数用尽、窗口外、无历史链接 | 只跳过非客观题/0%/无链接；其它客观题仍检查历史 | 提取继承答题过滤或把无链接作业卡成待处理 | 正常 |
| T-I14 | 提取失败收口 | 损坏题、Supabase 失败、待写入安全保存失败、检测失败课程 | 损坏题跳过；失败进入待处理；安全保存失败停止当前学生；结束文案不伪装成功 | 失败题重复补偿或显示完全完成 | 异常 |
| T-I15 | 模式隔离 | 提取模式存在答题模式待回写队列 | 只补偿提取队列，不消费答题校对/删题队列，不调用 AI 或提交 | 提取运行混入答题回写 | 边界 |

补充：内部代码已按真实 history.aspx 结构接入。真实提交后 AI 对题入库、题库错题删除和待回写补偿仍需产品 Playwright 完整作答场景验证，不能仅凭离线测试打勾。

## 用例：Playwright E2E 真机 —— TASK-T01

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 类型 |
|------|---------|---------|---------|------|
| T-E2E1 | 凭据已进 safeStorage | 产品 Playwright 登录并刷新课程 | 只扫我的课程，检测不点做作业 | 正常 |
| T-E2E2 | 有可做客观题 | 开答到提交 | 点深蓝确定，历史出现新记录 | 正常 |
| T-E2E3 | 弹微信码 | 用户扫完点验证完毕 | 同号其余预览刷新再做，不三张码 | 边界 |
| T-E2E4 | 并行=2 第三人 | 一人扫码暂停 | 仍占位，第三人排队 | 边界 |
| T-E2E5 | 工具选择 | 跑 E2E | 不使用 @电脑 或本机 Chrome 代替产品浏览器 | 异常 |
| T-E2E6 | 作答页自然或故意形成漏题 | 提交前及弹窗阶段 | 自动补答；若仍弹未作答警告则取消，历史无漏题卷 | 边界 |

> 验收标准：所有 P0 用例离线通过，并且 09 Playwright E2E 全部真实通过，才视为 development 3/4 完成候选。禁止用 @电脑 代替。缺 DOM 受阻必须写明，不得伪造通过。

## 精简稳定回归基线

| 编号 | 长期能力 | 最小回归命令/入口 | 通过标准 |
|---|---|---|---|
| REG-1 | 启动壳 | 开发期启动命令 | 窗口出现且非插件 |
| REG-2 | 凭据不泄漏 | 仓库搜索 Key 模式 | 无真实 Key/secret |
| REG-3 | 预览路径不混 | 选择器模块单测或静态检查 | assignment-preview 与 assignment/preview 分开 |
| REG-4 | 提交认历史 | 提交模块 | 成功条件不是弹窗关闭 |
| REG-5 | 名额不补位 | 池单测 | occupying_verify 不 released |
2026-09-18 当前增量：离线 P0 已为 20/20，新增实时课程/作业快照和已验证校对升级未验证导入题、已验证答案冲突保留旧值两项；vue-tsc、Vite 构建、git diff --check 已通过。单一产品进程实测已证明登录、课程扫描和作业分类前置链路；4.3、6.3、7.1–7.3、8.1、8.3 仍按 09 清单继续复测。

## 2026-09-18 实时反馈回归

| 编号 | 场景 | 预期 | 结果 |
|---|---|---|---|
| T-C1 | 课程检测开始 | 左栏对应课程立即显示检测中 | 通过：P0 |
| T-C2 | 课程检测完成 | 左栏写入真实课程状态，课程数量和选中课程保持一致 | 通过：P0 |
| T-C3 | 作业状态变化 | 中栏按选中课程即时显示作业分组、作业状态和题目来源 | 通过：P0 |
| T-C4 | 多课程交错更新 | 中栏不会被另一门课程的最新事件覆盖 | 通过：P0 |

本轮离线回归：npm run test:p0 20/20；npx vue-tsc --noEmit、npx vite build、git diff --check -- . 均通过。真实产品 E2E 的提交、校对和待回写证据仍按 TASK-T01 继续补齐。

## 2026-09-19 增量回归

后续 P0 24/24：新增“提交证据排除新建未交卷记录和日期格式变化”。本地 Chromium 集成 tests/answer-writeback.mts 覆盖 CDP fallback 读取值 0、延迟多选、额外选项拒绝，修复前失败后通过。构建和 diff-check 通过。

`npm run test:p0` 23/23：补充课程不能在部分作业完成时宣告完成、失败/待回写聚合、逐题快照按题号更新不重复。`npm run build` 通过。真实全部课程顺序与界面订阅仍需 G-ALL-01 至 G-ALL-05 证据。
# CHG-009 增量回归

## PRD 1.15 新增离线与集成用例（按细项执行中）

### T-C03 工作台、历史成绩和课程范围

- [ ] 同一状态事件只渲染一次，状态切换后旧文字消失。
- [x] 同一状态事件只渲染一次，状态切换后旧文字消失。
- [ ] 全部课程与可选课程对答题/提取形成四种组合，队列只含目标课程。
- [x] 多学生选择互不串号；未选课程即时标记且不入队。重新扫描清空旧选择、完全未选学生仍保留独立真机用例。
- [x] 执行开始后模式、选择和工作模式均锁定，停止后恢复。
- [ ] 历史成绩只读，覆盖正常、空、未批阅、未完成、仅续做、无查看链接和读取失败。
- [ ] 日志按 accountId+courseId 过滤，切换课程立即切换数据，敏感字段被拒绝。
- [ ] 历史记录按最新提交优先；开始前汇总已选学生数、课程数和未选学生；日志覆盖规定事件并使用五级颜色。

2026-09-21 回归：`node tests/e2e-course-selection.mjs` 通过两学生独立选择、未选课程排除、选中队列范围和停止后恢复编辑；`node tests/e2e-run-lock.mjs` 通过运行中 UI/IPC/模式锁；`npm run test:p0` 70/70。

### T-G02 程序内二维码

- [ ] 截图只包含真实二维码容器；PNG 只在内存和 IPC 流转。
- [ ] 复制写入图片剪贴板；刷新生成新版本并使旧版本失效。
- [ ] 无头模式不显示窗口；可视化模式不新开窗口。
- [ ] 未点击验证完毕或页面复核失败时，任务持续 occupying_verify。
- [ ] 同一学生多个页面只保留一个有效验证会话。
- [ ] 快照包含任务/课程/作业/截图时间/版本；真实刷新入口优先，无入口时重开验证弹窗；刷新后旧版本失效。
- [ ] 关闭二维码弹窗不释放验证占位、不恢复队列；图片未落盘且未写日志。

### T-I02 题库查看、删除和导入

- [x] 密码哈希校验、5 次失败、60 秒冻结、重启后仍冻结、成功清零。
- [x] 服务端分页、课程筛选、模糊搜索、精确搜索、清除和时间格式化。
- [x] 单删和批删二次确认；真实删除后回读不存在；部分失败逐条返回仍保留为独立异常用例。
- [x] 导入测试覆盖新增、重复、同题不同课、选项乱序、同答案、冲突、缺字段、空值、未知题型、非法 JSON、替换字符、写入/回读失败。
- [x] 清理仅删除本批 hash，回读为零，非本批抽样题目测试前后保持一致。

2026-09-21：P0 66/66；真实导入 E2E 使用独立批次，成功路径走真实 Supabase，写入/回读失败由主进程受控故障触发并确认 UI 显示失败。

### T-J02 AI 连通性

- [x] 最大同时请求数为 3，完成一个才补一个。
- [x] 每模型结果含状态、耗时、HTTP、失败分类和时间。
- [x] 单模型重测只更新该模型；正式答题顺序由独立模型清单回归保持不变。
- [x] 四种汇总结论边界正确。
- [ ] pending/queued/running 过程可见；限流结果显示 Retry-After 或实际等待时间。（queued/running 已实测；真实限流仍未自然触发）

### T-B03/T-D02 配置、窗口和 metrics

- [x] 设置保存广播后工作台和学生卡同步，无陈旧并发值。
- [x] 运行中渲染控件禁用，直接调用主进程修改也被拒绝。
- [x] 本轮调度始终使用启动快照，停止后可修改。
- [x] 无头/可视化只切换已有窗口，标签与实际可见性一致。
- [x] 浏览器数仅含 launching、occupied、occupying_verify；排队不计数。
- [x] CPU、系统内存、软件内存和压力状态来自真实采样而非演示数据。
- [ ] 任务结束仍保留 profile/登录态；采样失败显示 unavailable/error；设置广播失败保留旧值。（profile/登录态和设置广播已实测，采样失败专门场景待测）
- [x] 运行中点击恢复默认或直接调用对应 IPC 都不能改变本轮并发。

### T-T02 完整性门禁

- [ ] 建立“页面×功能×按钮×状态×正常/异常×回读”覆盖表，每一项都有用例 ID。
- [ ] 新增功能与现有四页全部交互无未接线、固定成功或无动作按钮。
- [ ] 每个失败项关联 TASK、复现证据、修复提交和复测结果。
- [ ] 扫码后用例单独保持待测，不与扫码前结果合并。
- [x] 业务实现开始即升级开发版本并更新 CHANGELOG；package、界面/构建元数据和报告版本一致。

- 错答案删除返回成功但读回旧答案仍在必须失败；读取异常不得视为不存在；其他学生已纠正的新答案不能误删。tests/p0.test.mts 已覆盖。
- 全链路集成验收见 09 的 G-ALL-11 至 G-ALL-16，离线测试不替代真实站点与云端结果。
2026-09-22 追加：工作台真实 E2E 固化主状态与当前动作分离，并在扫描过程中验证 current-action 真实切换；完整页面矩阵仍待逐项补测。
2026-09-22 追加：课程选择 E2E 真实通过两个学生独立选择、首个学生多选 2 门、桌面通知、未选课程排除、队列范围、停止后恢复编辑和重新扫描清空旧选择；显示模式 E2E 真实验证 headless:true/headless:false、运行中拒绝切换、停止后复用 profile。
2026-09-22 追加：工作台 E2E 先红后绿修复未选课程日志门禁；复测通过未选课程日志禁用、课程日志切换与颜色、敏感字段过滤、按钮动作/禁用原因和提取中快照对齐。
2026-09-22 追加设置页真实复测：14 个模型受控并发峰值 3，10 个成功、4 个失败（超时或空响应），单模型重测、保存失败回滚、设置广播、机器采样和弹窗关闭通过。
2026-09-22 追加工作台课程选择与学生账号回归：课程点击、取消后当前课程回退、学生/课程日志隔离、历史成绩和提取快照通过；学生账号新增、搜索、编辑取消/保存、工作台切换、明文显示和删除回收通过；设置页高于推荐值警告与保存后恢复通过。
2026-09-22 追加题库首页探针：无需输入门禁密码即可真实进入题库首页，确认仅显示导入/查看两张卡片和四项导航入口。
2026-09-22 追加阶段性测验历史成绩补测：真实阶段性测验作业历史按钮、IPC 读取和弹窗通过；返回 1 条 no_view 记录。工作台探针遍历两个学生的所有可读取历史作业，viewable、unfinished、no_view 真实 UI 通过；emptyHistoryChecked=false、failedHistoryChecked=false，暂无记录、未批阅和历史读取失败未自然出现，保留实现/受控分支及用户现场复核备注。
2026-09-22 追加工作台长状态回归：先红后绿验证主状态/当前动作最多两行、title 完整内容提示、min-w-0 布局约束，并重新通过工作台真实 E2E。
2026-09-22 追加课程选择 UI 真机回归：tests/e2e-course-selection.mjs 已改为通过 Patchright 点击课程卡片和“开始执行”，不再用 IPC 代替用户选课；输出 independentSelections=true、multiSelect=true、unselectedExcluded=true、queueScoped=true、stoppedAndEditable=true、rescanClearsSelection=true、partialStudentSkipped=true。
2026-09-22 追加题库导入真机回归：tests/e2e-bank-import.mjs 启动产品后通过真实 saveSettings 注入当前测试连接配置，独立批次输出 added=1、duplicate=1、courseMerged=1、shuffledMerged=1、conflict=1、skipped=11、writeFailure=1、readbackFailure=1、nonBatchProtected=true，finally 回读清理为零。
2026-09-22 追加题库门禁错误次数补测：独立 Patchright 真机连续输入四个错误值，界面依次显示剩余 4、3、2、1 次；分页、七列表格、长字段和查看页按钮的真实断言已接入测试脚本，当前轮未再次提供合法门禁输入，保留用户现场复核备注，不把账号密码或门禁密码写入测试。
2026-09-22 追加 B02 二维码前答题回归：使用真实账号、全部课程和真实“做作业”入口，分别以无头/有头启动；两种模式均在二维码快照准备完成后进入 occupying_verify / needs_verify，程序内二维码全部断言通过。修复并覆盖“验证态先于二维码快照”的异步竞态；扫码后的 J01～J18 仍单独保持待条件。

## 2026-09-22 CHG-026 UI 增量测试边界

### Codex E2E 必测

- U01-E01：使用模拟文本覆盖 Excel 复制内容、CSV、Tab、英文逗号、中文逗号、空格、换行和可选表头。
- U01-E02：覆盖批量解析预览、重复账号、空行、缺字段、格式错误、预计数量和二级确认。
- U01-E03：确认取消确认框不写入，确认后批量写入现有安全存储，测试只使用模拟账号。
- U01-E04：使用模拟课程列表确认固定高度、分页图标、手动翻页、当前执行课程变化自动跳页和边界页禁用。

2026-09-23 实测：`npm run test:e2e:chg026-ui` 通过。账号预览/取消/确认先红后绿，确认后安全存储回读 2 条、清理后 0 条；课程起始第 3 页、手动翻至第 2 页并返回、当前课程切换到第 1 页均通过。追加设置卡片间距断言先失败，调整左右列 DOM 后通过；同测确认隐藏公共字段和项目内 Logo 能加载。`npm run test:p0` 为 75/75，`npm run build` 通过。
2026-09-23 追加学生账号页桌面端一行三列断言：`tests/e2e-accounts.mjs` 检查 `#accounts-grid` 在 Electron 1440px 窗口下计算为 3 列；窄屏仍使用单列样式，但本轮只验证桌面布局。账号页顶部间距保持冻结基线。
2026-09-23 复测结果：`node --experimental-strip-types tests/e2e-accounts.mjs` 通过，桌面三列断言及新增、编辑取消/保存、工作台切换、删除回收均通过；`npm run test:p0` 75/75，`npm run test:e2e:chg026-ui` 通过，`npm run build` 通过。
2026-09-23 工作台更多学生菜单布局：账号 E2E 在旧布局先因菜单未与最后一行学生垂直对齐而失败；菜单移入最后一行右侧后，垂直中心差小于 2px、菜单位于学生胶囊右侧的断言通过。测试删除临时溢出账号并轮询回读至账号数恢复基线。
2026-09-23 OTA 流程回归：`node --experimental-strip-types tests/ota-flow.test.mts` 4/4 通过，覆盖开发版跳过、检查发现版本、下载失败重试、运行中禁止安装、停止后安装重启调用，以及 macOS DMG/ZIP 与 Windows NSIS 发布配置。
2026-09-23 U01-04 真实复测：3 个测试账号、账号并发 1 时 1 个占用、2 个排队；停止占用账号后排队账号接续；点击“全部停止”后运行态释放、按钮立即禁用、Electron 窗口保留。修复停止 IPC 缺少最终快照广播后，隔离 `test:e2e:run-lock` 通过。

边界记录：`npm run test:e2e:bank-import` 在写入测试题前的清理请求收到 HTTP 401，按用户指示忽略，不作为本轮阻塞；本轮未改题库业务规则。`npm run test:e2e:settings` 脚本通过，但 14 个模型均因本机未配置 API Key 而失败，不能作为真实模型连通证据。`npm run test:e2e:run-lock` 因本机没有已保存学生账号在前置检查退出，未伪造运行锁通过。

### 用户 UI 验收

- U01-U01：学生账号页空状态、桌面端一行三列学生卡和明文显示。
- U01-U02：设置页固定公共配置、仅显示硅基流动 API Key、两列布局。
- U01-U03：工作台学生行、等待学生菜单、居中并发状态、底部固定栏、全部停止。
- U01-U04：提取题库模式字段隐藏。
- U01-U05：自定义窗口按钮、更新检查入口、启动自动检查和品牌布局。

2026-09-23 用户确认上述 CHG-026 UI 项无问题；四页顶部间距按确认基线冻结。本轮不新增真机扫码矩阵，不改变已回写的 T15-J 结果。OTA 真实 Release 下载与安装仍需发布后验证，本轮不发布。

2026-09-22 用户最终验收：用户确认 T15-J01～J18 及整条业务链路均已完成真机实测；按用户要求不重复执行整套 T15-A～J。自动门禁保留 `npm run build`、P0 70/70、设置页 14 模型受控并发结果；账号重导入和一次真实网络中断均已记录在 09，不冒充功能通过。
