# T002 多学生调度与作答体验优化 · 评分记录

任务 ID：T002
评分时点：正式 PRD 1.0 转换后，PRD 已确认
评分日期：2026-09-24

## 五维分数

项目现状：8
修改规模：17
依赖与修改后影响：18
风险与回滚：20
验证复杂度：19
总分：82
评分分段：70–89 深度流程
流程等级：深度流程

## 评分证据

项目现状：8。现有 `src/views/HomeView.vue`、`electron/runner.ts`、`electron/core/ai-rate-limit.ts` 和账号页可定位入口，已有 P0 与产品 E2E；多选真实站点唯一根因仍未知，文档与本地测试证据分离。
修改规模：17。七项横跨作答、AI 节流、工作台配置、账号删除与动态调度，需要前端、IPC、主进程及持久化协同，不是局部单模块修改。
依赖与修改后影响：18。`loginAndRefresh` 当前在启动时固定账号集合，`stopStudent` 与浏览器名额释放共用状态；配置锁和运行队列影响多个调用方和共享并发限制。
风险与回滚：20。涉及全部账号删除、运行中账号调度和实际站点作业提交；误删或误提交难以靠撤销代码恢复，必须有确认、停止和禁止盲提交流程。
验证复杂度：19。需覆盖多选延迟保存、四项持久化、账号导入与停止竞态、浏览器占位、并发上限及产品 E2E；学校站点多选真机仍不可测，不能把模拟验证计作真实站点通过。

## 范围上限

本任务为已存在项目的复杂多模块增量改造，未新增三个独立产品模块、未重构整体架构，也未迁移技术栈；范围上限为深度流程。

## 安全门禁

命中数据删除、共享核心逻辑和多模块异步状态门禁，最低深度流程；不因第 1 项已先行修复而降为单点任务。

## 项目级完整条件

未命中新项目、三个新增独立功能模块、整体架构重构或大规模迁移；不是完整流程。

## 最终档位

深度流程（82/100）。涉及现有 UI 入口；用户已确认不单独设置 UI 目标，视觉补齐并入第 2/2 次开发目标。当前第 1/2 次只收口文档、任务和验收准备，真实学校站点、Windows 真机和正式 OTA 仍待用户验收。
