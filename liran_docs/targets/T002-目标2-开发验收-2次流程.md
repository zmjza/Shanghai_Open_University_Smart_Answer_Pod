你自行判断本任务是否需要多智能体协作；如需开启，只允许一个主智能体统筹，任一时刻最多同时运行五个子代理，子代理完成后必须及时回收，回收后可按需启用新的子代理，禁止子代理继续派生代理。

请直接开始执行开大智达舱 T002「多学生调度与作答体验优化」深度流程第 2/2 次目标。

任务 ID：T002
流程档位：深度流程
当前阶段：development-and-acceptance
当前目标：第 2/2 次，最后一次普通目标
版本目标：2.2.0
UI 决策：不创建独立 UI 目标，沿用现有视觉设计直接补齐。
发布目标：只推送 GitHub；不推送 Gitee。

必须先读取 AGENTS.md、docs/pitfalls/README.md、docs/pitfalls/general.md、正式 PRD、03/04/08/09 文档、页面结构、相关源码测试、Git 状态，以及 github-electron-release-ota 技能和发布清单。

本轮以正式 PRD 为唯一需求事实，完成 REQ-001～REQ-007 的实现审查、必要修复、现有视觉补齐、TDD/回归验证、构建、版本更新、提交、GitHub 推送、统一 Release 发布和远端资产核验。

扫码边界必须严格区分：
- 扫码前允许真实学校站点测试，但用户未提供账号密码前只能记录为待执行；收到凭据后最多实测到二维码和等待验证状态，未完成扫码确认不得进入作答、提交或批阅。
- 扫码后禁止真实扫码，必须用本地受控页面、固定响应或 mock 外部边界，保留真实 runner、验证闸门、作答、提交、最新历史、批阅回写和资源释放链路；内部通过不得写成真机扫码后通过。

重点验收：
- 多选保留原 locator.click() 首选，逐项等待保存；首选失败才用 checked DOM click 降级；结果未知或最终状态不一致时停止且不提交。
- AI 普通请求间隔适度增加，保留 429、Retry-After、冷却和模型降级。
- 唯一“将配置设置到全部”同步课程模式、执行环境、工作模式和重新答题次数；运行、验证、排队期间锁定，停止并释放后解锁。
- “全部删除”二次确认，运行和排队安全停止，账号集合变化重新确认，题库不受影响。
- 每个学生独立“开始”重新扫描并入队；全局运行时手动、Excel、粘贴新增账号自动运行或排队，重复账号不重复启动，服从并发上限。

至少运行：
- npm run test:p0
- node tests/multiple-answer.mjs
- node tests/t002-ui.mjs
- node tests/t002-scheduler.mjs
- npm run test:t002-post-scan
- npm run build
- git diff --check -- .
并执行项目已有相关 E2E、类型检查、完整 diff 审查和敏感文件检查。

同步更新 CHANGELOG.md、03-索引.md、04-开发追踪.md、08-测试用例.md、09-真机实测.md 和必要的避坑记录。CHANGELOG 必须写清版本、变更、验证和扫码前/扫码后限制。

发布前只提交已审核文件，不得提交 dist、dist-electron、node_modules、浏览器 profile、账号、密码、Cookie、日志或密钥。使用 Conventional Commit。
使用 npm run release:publish -- --notes "与 CHANGELOG.md 一致的 2.2.0 发布说明"，验证 Tag、Release 状态、更新说明、macOS ARM64 DMG/ZIP、Windows x64 NSIS、blockmap、manifest、SHA-256、SHA-512 和回下载哈希。
发布任何阶段失败都必须停在实际失败阶段，不得声称已发布，不得覆盖公开 Tag 或复用旧资产。

完成条件：所有已确认需求实现并通过内部验证；扫码前真实证据边界准确；扫码后内部完整链路通过；源码、构建、提交、GitHub 推送和 Release 资产核验全部成功。最后进入“待用户检查、待归档确认”，不得直接归档。

修改前评分：82/100
五维评分：项目现状 8、修改规模 17、依赖与修改后影响 18、风险与回滚 20、验证复杂度 19。
评分证据：详见 `liran_docs/requirements/T002-评分记录.md`；本任务跨作答保存、AI 节流、Electron IPC、账号删除、并发调度和 UI 状态，且包含不可逆删除与学校站点提交风险。

读取文件：项目 `AGENTS.md`、`docs/pitfalls/README.md`、`docs/pitfalls/general.md`、正式 PRD、需求整理、评分记录、`liran_docs/03-索引.md`、`04-开发追踪.md`、`08-测试用例.md`、`09-真机实测.md`、`开大自动答题页面结构.md`、相关源码、测试、构建配置、Git 状态，以及 `liran-plan` 和 `github-electron-release-ota` 技能文件。
允许修改：T002 已确认需求涉及的实现源码、现有视觉样式接线、测试、版本号、CHANGELOG、开发追踪、测试用例、真机边界、避坑记录和发布脚本；只推送 GitHub，版本为 2.2.0。扫码后只能修改内部测试边界或受控测试替身，不伪造手机扫码证据。
禁止修改：不创建独立 UI 目标；不删除原有多选首选回填方式；不把扫码后内部模拟写成真实扫码通过；未拿到账号密码前不做扫码前真实站点测试；不提交账号、密码、Cookie、浏览器 profile、日志、密钥、`dist/`、`dist-electron/` 或 `node_modules/`；不推送 Gitee，不直接归档。

验证：按本提示词中的测试、构建、`git diff --check -- .`、敏感文件扫描和完整 diff 审查执行；扫码前真实测试最多到二维码及等待验证状态，扫码后必须用本地受控页面、固定响应或 mock 外部边界跑通门户验证、二维码验证、作答、提交、最新历史、批阅回写、作业结束、浏览器释放和配置解锁。
状态回写：更新 `liran_docs/03-索引.md`、`04-开发追踪.md`、`08-测试用例.md`、`09-真机实测.md`、`CHANGELOG.md`、必要的 `docs/pitfalls/`，并在目标记录中写明内部通过、扫码前待账号密码、扫码后禁止真实扫码、Windows 真机和正式 OTA 安装重启的验收边界。
下一阶段：本目标完成后进入“待用户检查和待归档确认”；用户检查期间不再执行当前目标。用户发现问题时回到问题回写、修复和复测；提出新增需求或优化时回到需求整理或需求变更流程。

交接合同：当前目标完成开发、测试、验收、问题回写和必要修复；当前目标完成并已回收；`goal_status=reclaimed`；项目状态：待用户检查；资料归档状态：待归档确认；当前目标暂不执行正式归档；不清理活动文档；进入待用户检查和待归档确认。只有用户明确回复“归档”后才调用正式归档模板。归档失败不得重新打开普通目标。
